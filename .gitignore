# Python / uv
.venv/
__pycache__/
*.pyc
.python-version.local

# Heavy artifacts — keep off git, live on the 1.2T data volume
data/
outputs/
checkpoints/
runs/
wandb/
*.ckpt
*.pt
*.safetensors

# Scratch
tmp/
.tmx/
.ipynb_checkpoints/

# OS / editor
.DS_Store

# Obsidian — track shared vault config, ignore per-machine pane/cursor state
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# Claude Code per-worktree session state (wrap snapshot, etc.) — not shared
.claude/state/
.claude/worktrees/

# Teach lesson HTML renders — disposable views (rebuild with render_lesson.py)
docs/learning/lessons/*.html
