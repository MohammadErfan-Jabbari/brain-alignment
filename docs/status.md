---
title: "Project status"
tags: [status]
aliases: [current-status]
---

# Project status

**Updated:** 2026-09-02

**Manuscript:** `manuscript-sync-pending, deliberately: submission-only work until the deadline`. Erfan directed on 2026-09-01, with under 24 hours to the thesis deadline, that all remaining work go into `docs/manuscript/submission/` alone and that `docs/manuscript/rewrite/` be left untouched. The two trees were byte-identical except for format-only lines at commit `5688de9`, verified by a clean twin audit over 41 file pairs, so that commit is the exact point they diverge. Every submission-only edit after it must be flowed back into the canonical tree before `rewrite/` is an authority again. This is a deliberate, recorded suspension of the canonical-first rule for a deadline, not an authority inversion. The thesis in `docs/manuscript/rewrite/` remains the sole scientific-interpretation authority; `docs/manuscript/submission/` is its university-template derivative, now 60 pages with zero overfull boxes after the Section 1 to 7 cuts, the appendix pass, and the float-placement repair. The Section 5 residual prose findings, the four verification-layer defects, and the UC3M supervisor's own remarks are all closed in both trees without changing a scientific verdict or number. The supervisor's reported Figure 10b contradiction was live in the canonical tree, where the bar labels were hardcoded `24 passed` and `30 failed` while the bar widths came from the corrected keys; the labels are now keys and the figure reads `37 = 20 + 16 + 1`. Both trees pass `manuscript_check.py`, the derivative passes `--share-ready` with no `\gap`, and `prose_lint.py` is clean. The only exceptions to the suspension are approved edits to `rewrite/question-tree.md`, made because the tree is the target every audit reads, so a prose fix alone would be undone. On 2026-09-01 Erfan approved leaf 5 of the Introduction, from "ordered evidence-chain stages" to "claim-specific dependencies", because that leaf was instructing the Introduction to assert a universal stage order that `manuscript/AGENTS.md` and Section 2.4 both refuse. Later the same day he approved four Section 2 edits: the section title became "Background and Related Work" to match the heading he had chosen; leaf 2 of Section 2.4 was struck because Section 2.2 owns target uptake and retained-student movement; the 2.4 ownership note was rewritten to say so; and leaves 2.3.4 and 2.4.1 were narrowed after the assay controls and the projection paragraph moved to the subsections that own them. In the Introduction cut he had licensed, leaves 4 to 6 were narrowed: leaf 4 no longer names the encoding and compression literatures, which Section 2.3 owns; leaf 5 now asks for the exact tested claim and each route's comparator; leaf 6 asks for the answer to each of those dependencies. In the Section 3 cut under the same license, the tree's Section 3 was retitled "Methods" to match the heading; leaf 3.2.4 was narrowed to reliability context and the shared readout protocol; 3.3.1 dropped the training budget; a new leaf 3.3.7 owns the practical-utility evaluation, which moved into the recorded-response subsection; 3.4.1 and 3.4.5 dropped their block-permuted and utility clauses; 3.5.1 was struck and the remaining 3.5 leaves narrowed to quality rules, aggregation with named test families, bounds, and the status decision rules; and the 3.4 ownership note gained saved-student target retention. Nothing else in `rewrite/` was touched. Its professor-approved title is *Evaluating Brain Alignment as a Training Signal for Language Models* ([D066](decisions/decisions.md)).

**Legacy layers removed from HEAD (2026-08-25):** the `extended/` legacy snapshot and the frozen public v0.9 cut are preserved only in Git history (last commits: `extended/` at `7c445b0`, `public/v0.9/` at `6026021`). Do not recreate them as directories; cite the commits when historical access is needed.

## Research state

| Rung | State | Owning evidence | Manuscript location (legacy extended numbering) |
| --- | --- | --- | --- |
| Q0 / A2 | CONDITIONAL PASS: trained-model controlled predictivity is supported under the executed assays; E002's learned-parameter attribution from the trained-minus-untrained gap is unresolved because the static nuisance block was model-specific; no E006 participant/population inference | [E002](experiments/E002_tuckute-encoding-feasibility.md), [E006](experiments/E006_lebel-voxelwise-feasibility.md) | Methods; Results §4.1 |
| Q1 | PARTIAL: plain KD shows alignment headroom, but KD-specific shedding is not isolated from LM quality | [E003](experiments/E003_kd-alignment-preservation.md) | Results §4.2 |
| Q2 | NO DEMONSTRATED INTERVENTION EFFECT at the valid inference unit; small effects are not ruled out | [E004](experiments/E004_brain-loss-lever-test.md) | Results §4.3 |
| Q3 | NO DEMONSTRATED PARTICIPANT-GENERAL GAIN across tested ROI/objective variants; E025's direct TRIBE-minus-KD contrast is near zero. Its preregistered relative upper bound falls below the internal `+0.002` continuation threshold, but E026 shows that the saved cross-target comparator fails pre-outcome and KD-baseline comparability, so the relative contrast is not content-identifying. E030's 50-component linear target-projection assay does not meet its internal continuation rule: aligned TRIBE above nuisance is `-0.004494`, with family-95% upper endpoint `+0.001707 < +0.002`; the frozen-twin contrast remains unresolved | [E008](experiments/E008_per-participant-f1-solidification.md), [E011](experiments/E011_strong-regime-per-individual.md), [E013](experiments/E013_open-frontier-multisubject-naturalistic.md), [E025](experiments/E025_participant-e016-biological-transfer.md), [E026](experiments/E026_tribe-textfeat-target-comparability.md), [E030](experiments/E030_exact-substrate-transport-diagnostic.md) | Results §4.4 and §4.6 through E030 |
| Q4 / A3 | PRACTICAL UTILITY NOT DEMONSTRATED: the controlled-predictivity prerequisite was seed-unstable and the downstream utility contrasts remain descriptive; the observed MDE is a sensitivity quantity, not a null or equivalence test; gaze privileged-information substrate failed its preconditions | [E009](experiments/E009_a3-practical-payoff.md), [E024](experiments/E024_zuco-lupi-sample-efficiency.md) | Results §4.7; Discussion |
| Q5 | NOT STARTED and currently moot because it depends on a Q3 benefit | No active E record | Deliberately excluded |

## Active work

- New experiment development is closed by Erfan's direction ([D065](decisions/decisions.md), 2026-08-25). The prospective program below (E033, E032, E028 exact-lane resolution, queued dimensionality work) is paused, not executed, unless Erfan reopens it. The university-template derivative is rebuilt, its cover-page fields are confirmed, and it is ready for Erfan's read.
- E033 remains design-locked but inactive under D065. Its prospective L12-versus-final-hidden recorded-response placement test has not opened an outcome and has no compute license unless Erfan reopens development.
- Erfan's reader-first review is complete through Results. The rewrite distinguishes claim-specific dependencies, uses explicit Methods and appendix handoffs, and applies the fixed brain-response terminology and central `acronyms.tex` registry throughout. Discussion through Conclusion now have a bounded argument-review hold before their prose passes.
- The rewrite manuscript is the canonical thesis and current scientific-interpretation authority. All future manuscript writing, checks, builds, and supervisor-facing exports use `docs/manuscript/rewrite/`. The legacy extended manuscript and frozen public v0.9 cut are Git-history-only layers (see the manuscript note above).
- E025 is complete and result-oracle clean. All language-quality, target-learning, and representation-movement gates passed. The direct TRIBE-minus-KD mean is `-0.000014`, CI95 `[-0.000739,+0.000711]`. The preregistered participant-first relative contrast is `+0.000136`, CI95 `[-0.000505,+0.000777]`, with one-sided UCB `+0.000653 < +0.002`; this relative estimand does not identify content after E026. UID 837 carries the sole clear participant positive in both views.
- E026 is complete and independently audited. In the corrected identification set, 16/37 checks fail, 20 pass, and one is unresolved. The decisive mismatches are KD baseline/headroom, covariance spectrum/effective rank, and low-level nuisance predictability. Separately, 14/18 post-training diagnostic margins depart and four remain within margin; these outcomes describe the realized interventions but do not determine comparator identification. The raw cross-target difference remains non-identifying without invalidating TRIBE's within-target learnability or E025's direct TRIBE-minus-KD contrast.
- E030 is complete, author-confirmed, independently recomputed, and synchronized into the canonical rewrite manuscript. All `270/360/6,480` C1/C2/C3 rows, `748` independently recomputed load-bearing leaves, and the full provenance chain are panel-clean. The target is strongly extractable from TRIBE students (`M=+0.040725`, family-95% CI `[+0.039053,+0.042398]`), but its aligned unique participant predictivity above nuisances fails first. The frozen-twin contrast, incremental retention over KD, and all bridge contrasts remain unresolved. No post-outcome E030 variant is licensed.
- E031 is complete and independently audited. C1 is `NO CONSENSUS INCREMENT`: the residualized consensus target has fixed-cohort predictivity `+0.004379`, but its improvement over uniform averaging is `+0.000936`, below the frozen `+0.002` gate. C2 is `BUILDER INSTRUMENT FLOOR`: \(R_{\mathrm{builder}}=0.000116<0.01\), so its five independent evaluation repeats remained sealed. These classifications reject the frozen consensus-weighting and whole-cortex-median linear instruments, not denoising in general. No target-gradient, repeat-aware LeBel, or student-training license was issued. The load-bearing values and classifications are audit-clean; Erfan's confirmation remains required before manuscript synchronization.
- H002 and E032 now define the next prospective information-recovery test. E032 freezes a one-component, shrinkage-regularized CorrCA filter on Kymata English participant E2, with builder runs 1 and 3, evaluation runs 2 and 4, MEG gradiometers primary, EEG secondary, PCA-1 and shifted controls, and builder-only decisions. Live metadata support four runs and eight exposures for E2 but expose an event-duration contradiction, an English machine/calibration contradiction, 64 rather than 70 EEG channels, and a CC BY 4.0 versus CC0 license mismatch. Raw-header, `STI101`, complete-window, channel, and calibration checks therefore precede every neural score. The exact acquisition-integrity slice compiles, passes 14 synthetic tests, and is independently `PANEL-CLEAN`; participant neural FIF values remain unopened and authorization is false. Builder, score, and analysis stages remain deliberately unimplemented, so E032 is not endpoint-ready.
- The prospective dimensionality experiment remains queued behind the current biological-information design and manuscript review. Its estimand, controls, inference, and anti-confound contract remain unchanged and no compute is licensed.
- The working conference route remains a focused methodological/falsification paper built around source-target validity, target uptake, student movement, control identification, biological transfer, and utility. E028 is the primary external gate; a controlled reproduction of Xiao et al. 2026 is the lower-dependency feasibility backup. A theory-first route remains HOLD, and the positive-mechanism route is closed.
- The [E028 author-artifact request](references/e028-vaidya-artifact-request-email.md) was sent on 2026-07-22 to the corresponding author with both coauthors copied. The first response checkpoint is 2026-08-05; one concise follow-up is permitted, and 2026-08-12 is the exact-lane decision checkpoint. Silence means exact reproduction is unavailable, not that the reported result failed.
- E028's outcome-blind synthetic Stage-1 corrected-evaluator slice is implemented and independently panel-clean. Two 108-cell replays were byte-identical; schema, support, nuisance-sharing, pooled aggregation, exact sign-flip inversion, sentinels, and adversarial fixtures passed. The deliberately positive synthetic branch passed while `stage2_licensed=false` and `ready_for_endpoint_access=false`. This is plumbing evidence only. The production provider, RFC 8785 chain, factual dimensions, production-shape benchmark, exact lane, and endpoint-bundle review remain absent.
- The package strategy is recorded in [D057](decisions/decisions.md) and traces to the non-authoritative [initial Pro audit](external-reviews/chatgpt-pro/2026-07-14-01-initial-manuscript-strategy-audit.md) and [corrected Pro audit](external-reviews/chatgpt-pro/2026-07-14-02-corrected-research-audit.md).

## Conference packages

| Package | Identity | State | Next gate |
| --- | --- | --- | --- |
| A | Methodological/falsification protocol | **Strengthened default route; internal dissociation complete, external gate missing** | Run a 10-business-day E028 access/executable feasibility gate in parallel with a smallest-setting Xiao reproduction design; choose the external lane before expensive compute |
| B | Conditional theoretical boundary | **HOLD** | Generic theorems are occupied; proceed only if an observable directional certificate prospectively predicts both failure and success regimes |
| C | Positive brain-guided mechanism | **Closed; E025 continuation rule failed** | No E027: E025 failed the conjunctive participant-positive continuation rule, and direct TRIBE-minus-KD is near zero for this cohort |

## Blockers

- No scientific-evidence or argument-review blocker remains. The four verification-layer defects found on 2026-09-01 are closed: the two over-attributed citations were narrowed to what their notes support, `lazic2010` now has a canonical note, and the derivative-only Results clause was flowed up to the canonical tree in plain language, so both trees now carry it identically. The Section 5 prose swarm is parked by Erfan's 2026-08-31 stop after four fail-loud infrastructure stops (GLM output-cap deaths; OpenAI-Codex usage limit; kimi-coding weekly quota; counter output-format drift, since fixed by the v3.4 gate hardening). The swarm implementation was retired by [D068](decisions/decisions.md); the recorded Section 5 judgment calls are adjudicated under `/review` instead, and the routing lessons are kept in [`references/section-review-swarm.md`](references/section-review-swarm.md). Creating a new immutable public cut remains a separate decision requiring Erfan's approval.
- E028's exact lane remains blocked by author-controlled code, checkpoints, masks/folds, dataset facts, and result-regeneration artifacts. Its synthetic Stage-1 development slice is implemented, tested, and independently `PANEL-CLEAN`, but the production provider/chain/dimension benchmark and sealed endpoint-bundle review are absent; `ENDPOINT-READY: NO` remains unchanged.
- A top-AI-conference claim is not currently supported. It requires a non-inconclusive prospective external adjudication and a reusable intervention-audit implementation; the unchanged thesis should not consume a top-tier main-track cycle.

## Next actions

1. Erfan is reading the derivative section by section and returning feedback as he goes; apply what each round calls for. The abstract is settled at 350 words (`e35033d`) and Section 1 is rebuilt against the claim-dependency contract (`852f972`), which corrected an inverted endpoint sign, an asserted null, an unsupported four-citation claim, and the universal stage order. Section 2 was reviewed and then cut with the Elon requirement-deletion pass, from 2,393 to 1,895 words with all 22 citations kept, five factual defects fixed, and the claim-dependency figure relabelled. The Introduction then had the same cut pass, from 1,130 to 1,046 words including markup: the four-literature inventory, a restated definition, the threshold definition, and the Tuckute retention sub-verdict went; the exact tested claim now opens the dependencies paragraph, and the recorded-response route gained the permuted-control dependency it lacked. One open question for Erfan: the abstract says the control was "shuffled onto the wrong sentences", which reads as a derangement, while the recorded permutation can leave blocks in place. Section 3 then had the same review-then-cut pass, from 671 to 577 source lines and the PDF from 68 to 67 pages: the auxiliary-loss and participant-estimate displays, the seed conventions, the duplicated adequacy rule, and the repeated fold and reliability sentences went; the comparator paragraph now names the three matched-property groups with the unretained gradient axis marked as such, the tests sentence names each test family against its record, the status paragraph defines all four labels, and the practical-utility evaluation moved into the recorded-response subsection. The attribution-endpoint sentence rests on E025 lines that would not print, so it is verified only against the display it replaced. Section 4 then had the same review-then-cut pass, from 621 to 549 source lines and 22.1% fewer words, with the PDF at 66 pages. The review round corrected the E002 narrowing wording, the bootstrap unit, and the one-sided upper bound's placement, added the observed-variance detectable-effect value so Section 6's pointer resolves, gave the variants table its fifth row to close Section 3's five-variant count, and reduced the status vocabulary from six labels to five by striking *inconclusive*, rewriting the *unresolved* key to Section 3's definition first so no verdict moved. The cut round ran four independent thinking-panel methods against a declared per-subsection word budget; they converged between 19.8 and 22.3 percent. Three verification agents then read the cut, including one reading the section cold, and found the expected second-order damage: the cut had orphaned two topic sentences, a warm-route antecedent the status table promises, the practical-utility contrast and its quality matching, and the direct-transfer contrast the equation depends on. All 24 repairs are applied and cost 11 words. Two verification claims were rejected on the evidence: the composite figure labels are unused in source but the subfigure references already render panel letters, and the three roles said to lack a verdict location are supporting analyses, not rows in the thirteen-role status table. Erfan then authorized all three routed items: four realized exact sign tests for participant estimands were declared and wired into 4.3, 4.4 and 4.6, the E016 interval retagged to E025 which owns it, and the capable-band statistics condensed to a pointer at Appendix F. Section 5 then had review and cut combined into one round, because it carries no keyed value and no citation, so its only factual liability is a sentence claiming more than its record licenses, which in an interpretive section is usually the sentence that should not exist. Verification stayed a separate round for the reason it was separate before, which has nothing to do with numbers: a deletion pass cannot see the damage it does. The question tree already declared the budget at 1,200 to 1,300 words against an actual 1,540, so the target came from the authority instead of being invented for the pass. Five agents found one reversed result, 05:24 reported the practical-utility means as higher for the recorded-response arm when the endpoint is a perplexity ratio whose negative values favor it; a borrowed construct, E011 measured unique R-squared and ran no movement audit; a collapsed status, the composed-path diagnostic is unresolved and not a null; a sixth status label; a list that promised four explanations and gave five; and a marker run of nine on one sentence, three of which do not belong, including E014 whose own record forbids the use. Most of the cut came from re-derivation rather than wording: 07:19 is verbatim identical to 05:56 and 02:167 states 05:87 word for word. Two verifiers then read the result and every finding was damage the cut created, three of them in the parent's own replacement text, including a compression that claimed support 04:207 denies and a marker slide onto a sentence E026 does not own. 1,540 to 1,224 words, 20.5 percent, at 66 pages. Tree leaf 5.2.2 was narrowed and then restored: the deleted sentences were third copies of the comparator catalog and had to go, but applying those rules to prior positive studies is a post-results judgment no other section performs. Erfan then asked why the PDF wasted so much vertical space and whether the NeurIPS format caused it. It did not: three causes compounded, and two were ours. Every float was `[H]`, which cannot move, so a tall one that did not fit ended its page early; NeurIPS sets `\flushbottom`, which then spread the slack into mid-page gaps instead of leaving it at the foot; and `\FloatBarrier` calls at section boundaries were harmless while floats were pinned but blocking once they could move. Making the floats `[tbp]`, loosening the placement fractions, and dropping three barriers took 66 pages to 61 with no prose changed, all 44 floats still in page order and no undefined reference. Hand-moving text before figures was declined and the reason given: that is manual repagination, and it breaks on the next edit. Erfan then set a standing preference that a table must never split when it fits on one page, so seven of the eight `longtable`s became non-breaking floats; only the artifact-provenance table still breaks by necessity. The conversion first rendered four tables at full size, because those tables set their font in a `\begingroup` before the environment and `\@floatboxreset` resets it inside a float. That was caught because three tables reported byte-identical overflow to five decimals, which is not a small effect but no effect; the size setup now lives inside each float. Section 6 then had review and cut combined for the same reason Section 5 did, and verification again kept separate. 1,131 to 798 words, 29.4 percent, deeper than the 20 percent budget because one ten-sentence passage was almost entirely re-narration of Section 4. The load-bearing finding was an overclaim only the adversarial agent caught: the section credited the direct biological-transfer contrast with a threshold exclusion belonging to the attribution endpoint, since E025's primary contrast is the attribution one and 04:422 attaches the upper bound there before 04:424 calls that contrast non-identifying. The evidence auditor disagreed, noting both bounds sit below the threshold anyway; it was right about the arithmetic and wrong about the claim, because Section 4 never performed that read and Section 6 performing it would be the second results narrative the tree forbids. Verification found three defects and all three were in the parent's own replacement text, including a regression where a bound committed deliberately in `bfa9a4e` was overwritten by an unaudited causal claim singling out model family among several route differences; `git log -S` before undoing is what caught it. The marker audit passed 33 of 33 and confirmed the reassignment independently. A measurement bug was also fixed: the word counter skipped every line starting with a macro and undercounted this section by 22 words, though the Section 5 percentage is unaffected because both versions used the same method. Section 7 then had the same combined round, and it is the one section where the cut was deliberately shallow: 408 to 373 words, because the conclusion is the final answer and three sentences were made longer for correctness. Four of its five defects were vocabulary failures at the point where the reader can least afford them. It called the overall answer inconclusive, a sixth label three lines after the closed set of five; it delivered the non-identifying disposition only in paraphrase, so the word appeared zero times in the section the governing question is answered in; it paraphrased the utility disposition although 04:99 assigns both labels to two different objects; and it renamed the frozen continuation rule an improvement threshold. The fifth was found only by the marker audit and only because it was reading records rather than prose: the contribution sentence claimed a demonstrated ability to change the student through training with no route scope, while E011 and E013 record the recorded-route lever failing to take hold. Verification returned one finding and it was again in the parent's own replacement text, the fourth section running: a rewrite meant to fix a sentence that promised an ordering and never named one instead asserted the requirements are independent, which contradicts the three must-pass-first arrows in the claim-dependency figure. The premise for that rewrite came from an agent and was not checked, which is the same failure the Section 6 regression recorded. Two agent findings were rejected on the evidence and two orphan claims in the parent's own briefing were shown false by an agent that grepped them, so the accounting behind two earlier deletions was looser than stated although neither sentence changes. Every main-text section has now been reviewed and cut. The appendices then had their own pass, and it was the first one Erfan directed by content rather than by method: he said Table 8, the artifact-provenance longtable, was unnecessary for a master's thesis, and asked whether the class was wrong rather than the instance. It was. The repo's own attention budget already excludes machine-level artifact metadata and the root contract already binds each digest in its owning E record, so 26 rows and 59 SHA-256 digests a reader cannot recompute went, taking Appendix A from 313 to 150 lines and removing the last table that split across pages. The class check found nothing else: the remaining paths in B and C are reproduction pointers a reader can act on. Two independent auditors then read A, B and E against a 108-item floor and F against a 223-item floor, 294 checked, and found eight record mismatches. The sharpest was a frozen rule reproduced without its operative bound: the retained-student movement gate required linear-CKA distance above ten times the duplicate-extraction noise, and the same sentence says that noise reproduced exactly, so the stated criterion was zero; E025 fixes it at max(1e-6, ten times noise) and all 96 gate rows record 1e-06 as the threshold applied. The others were a crosswalk row calling E016 settled while E016 carries four required claim limits and Appendix A's own second table lists it as superseded, imageability claimed as primary for Tuckute where E004 keeps it a labeled sensitivity, a synthetic timeline built per token where the record and the builder both work per word, a weight sweep reported as showing no gain when one arm did clear at a quality cost, a voxelwise sweep credited with the unanchored run's two-degraded-models finding, a reading-time result reported as signal prediction which E021 twice forbids calling a success, and a quality-curve screen described as searching for varying alignment when it searches for flat alignment and did find one qualifying pair below the required quality range. Three declared intervals the main text already uses were restored where F restated the estimate bare, a reconstructed 24,000-step count that appears in no record went, and Appendix C gained the practical-utility training grid it never carried. Verification then found eight more defects, six of them in the parent's replacement text, which is now the fifth section running. The largest was second-order: cutting the table left eight pointers aimed at a ledger that no longer exists, including C's own claim that Appendix A owns the stated omissions, which were stated only in the deleted paragraph. A provenance audit confirmed all nine record corrections and found five more, four of them collateral, including a replacement sentence that had turned a filtered claim into a universal one E023 falsifies by binding no digest for an artifact whose result is reported. One proposed repair was rejected on the evidence: the reading-time sentence had genuinely lost its object, but the suggested replacement restored the signal-prediction reading that had just been removed. Two contradictions are routed to /interpret rather than fixed: E003 records the learning rate as matched across arms while the runner carries separate warm and cold values, and E030 states its decision rule as a simultaneous one-sided upper bound while its results table reports a two-sided interval and adjudicates with that endpoint, so 04:293 and D:311 each match a different sentence of the same record. Two apparatus faults were also found and fixed. The float constants added in 3a26992 had been failing manuscript_check on four layout lines ever since, now exempted the same way arraystretch already was and verified against a fresh fixture. More seriously, manuscript_check runs a full latexmk, biber and biblatex are mismatched in this environment, and the combination silently replaced the PDF with a bibliography-less 65-page build that was committed in 717a9a7 and fixed in 40505b7; the committed .bbl is the only working copy and cannot be regenerated here. The trap is recorded in submission/AGENTS.md. The eleven other committed PDFs were audited and intact. The manuscript is 60 pages, both gates pass, prose_lint is clean across all six appendices, and the A_evidence_provenance soft warning is gone: its trigger was the word lever, not the ladder and rung the message named.
2. Open and parked by Erfan: Pablo Martinez Olmos asked on 2026-08-24 for a main document of 10 to 15 pages with everything else as supplementary material, saying a 70-page thesis was too much to go through. The main text is 34 pages. The regulation sets no maximum, so this is a supervisor preference rather than a rule, and Erfan chose on 2026-09-01 not to act on it yet.
3. Also parked by Erfan: about sixteen appendix findings from the 2026-09-01 cold reads name reimplementation details that are unstated rather than wrong, such as the bootstrap resampling unit and the Bonferroni family size. Erfan directed that only the cheaply sourced ones be filled; five were, and the rest stand.
4. After supervisor feedback, decide whether to create a new immutable public cut.

Former experiment next actions (E033 implementation, E031 confirmation with E032 authorization, E028 lane resolution) are closed under D065 unless Erfan reopens them.

## Related

- [Methodology and authority contract](03-methodology.md)
- [Canonical thesis source](manuscript/rewrite/main-rewrite.tex)
- [D057 conference package decision](decisions/decisions.md)
