# Upspeed — read first, write last

**Last updated:** 2026-06-22 (S34 — **/meta: audited the hook stack (no blocking conflicts; ECC's dangerous gate already disabled via `ECC_DISABLED_HOOKS`) and, per Erfan, swapped the `## graphify` section in `CLAUDE.md` for context-mode's routing rules + deleted the graphify-only `.claude/CLAUDE.md`. NO science change — the live thread below is untouched, still mid manuscript-clean at §1 ¶1.** Prior research state, S33 — **/write: started a deep paragraph-by-paragraph clean of the extended manuscript with Erfan. Finalized the ABSTRACT (conference register, single paragraph, no numbers/CIs, no em-dashes, results deferred to a `\gap`) + fixed a real hole in the em-dash guardrail (L057). NO experiment, NO science number, NO rung change — Q0–Q5 stand exactly as S31.** The clean is in progress; only the abstract is done. This wrap also reconciled the unwrapped S32/D047 session and a doubly-used L055.)

> **Canonical Q-rung state lives in [`ladder.md`](ladder.md)** (Q0–Q5 unchanged). **How to operate = [`operating-map.md`](operating-map.md)** (the 8 stances). With no task, run `/orient`.

## What this session did
- **Began the extended-manuscript deep clean (`/write`).** Mode: paragraph-by-paragraph with Erfan — I evaluate/draft, run the suite, he pushes back, converge, write to the file, move on. Only the **abstract** is finalized so far.
- **Abstract, finalized.** Rewrote into the register of real ICLR brain-LM abstracts (read Moussa-2025 + Oota-2026 abstracts from `data/papers/` for the actual style): active voice, the inversion as the hook, weak-prior hypothesis kept. **Dropped the numeric gaps + CIs** (Erfan's call — qualitative headline only; numbers live in Results). **Deferred the whole results summary** to a `\gap` (refilled at the checkpoint that consolidates R08–R14, closing on a payoff line). Single paragraph, em-dash-free. In `main-extended.tex`; PDF rebuilt.
- **Em-dash guardrail fixed (L057).** `ai_tell_lint` matched only the unicode `—`, so LaTeX `---` slipped through; budget was 3 not 0. Now catches `---` in `.tex`, budget 0. `writing-style.md` updated. Regression-tested.
- **Records reconciliation (this wrap).** Last session was actually **S32** (the D047 convergence-gate work, committed `bf78142` but never wrapped — no timeline, ladder left at S31); this session is **S33**. Also **L055 was used twice** (S31 LaTeX-build + S32 quality-gate); renumbered the LaTeX one to **L056**, quality-gate keeps L055. A short S32 timeline stub was backfilled.
- Fixed a `/wrap`-paste corruption at `main-extended.tex:32`.

## What's next (Erfan's call)
- **Continue the manuscript clean (`/write`), paragraph by paragraph.** Next unit = **§1 Introduction ¶1** (split the long sentence-3; fix "rewards representations the brain would predict"; de-echo the abstract opening). Then **§1 ¶2** carries the big decision: push the weak-prior/MAP formalism down to Methods §3.3 and keep only the intuition + falsifiable prediction in the intro (Erfan leans this way). Then §2 related-work (the "empty cell" stated 3× — dedup), §3 methods, §4 results (incl. **naming the E006 CI unit** in the `tab:vox` caption: bootstrap over the 11,442 reliable voxels).
- **Live science thread (unchanged from S25):** Q4 sample-efficiency E024 (re-substrate to higher-N gaze → synthetic-PI MDE positive-control → re-gate → build). Q2 stays ❌; Q3 full-FT door stays CLOSED (n=6 too small, D042/L051).

## Blockers / open loops
- **Manuscript clean is incomplete.** Only the abstract is done; §1–§4 still need the pass. `main-extended.tex` carries an **accepted-residual** register sign-off (D047), NOT a clean verdict — the real ≥2-fresh-auditor clean verdict runs at the END of the full pass. Do not treat current bytes as register-clean.
- **`taste-reader` subagent not yet built.** Erfan wants a dedicated opus readability/taste agent (`.claude/agents/taste-reader.md`), distinct from `prose-register-auditor`. Run inline per paragraph until built. (tasks.md)
- **`kill-gated` is only soft-caught** by the linter (METAPHOR_RE, exit-neutral). Erfan wants it gone from all prose; promote to a hard ban is an open option. (tasks.md)
- **Pre-existing (unchanged):** `R06:96` bare `+0.0280` needs `[E0nn]`/`\gap`; Firecrawl MCP pending approval; ZuCo 1.0 clone (`data/zuco1`, carried S25+, bare nohup, verify with `du -sh data/zuco1`). The S25-carried stragglers are now resolved: `supervisor-email_2026-06.md` + `2026-06-20-report-R07.md` committed (S34); `.claude/worktrees/` gitignored.

## Key facts
- **Per finalized paragraph (Erfan's rule, S32):** run the full `/write` path on that paragraph's text — `run_checks` + `prose-register-auditor` + the taste-reader — and report ok/not before locking it. Write each finalized paragraph straight into `main-extended.tex` (not a scratch file); the D047 gate gets an accepted-residual re-stamp per edit (`record_register_verdict.py --group manuscript-extended --accept-residual`, run from REPO ROOT), with the real clean verdict at pass end.
- **No em-dashes in any manuscript/report** (Erfan, standing rule; now linter-enforced at budget 0, catches `---`). "kill-gated" banned from prose.
- **Abstract style anchor:** real ICLR brain-LM abstracts in `data/papers/` (Moussa, Oota, bilgin) — active "we", explicit question, qualitative results, no meta-framing about "the literature".
- **LaTeX build (L056):** `cd docs/manuscript/extended && PATH=~/.local/bin:$PATH tectonic -X compile main-extended.tex --keep-intermediates`. biber 2.17 in `~/.local/bin` shadows apt's 2.19. `.tex/.pdf/.bbl` committed; `.aux/.bcf/.toc/.out` gitignored.
- **Run code:** `uv run`; `export HF_HOME=/home/centcom/data/hf-cache`; 4× L40S. Git: `main`, push only when asked.
- **Apparatus (S34):** `CLAUDE.md` now carries context-mode's routing rules in place of graphify; context-mode also auto-injects those rules each session via its `SessionStart` hook (so they're intentionally duplicated, and now also reach subagents via `CLAUDE.md`). graphify skill/`.graphifyignore`/`graphify-out/` still exist — only the instruction files were cleaned. Hook stack audited: no blocking conflicts.
