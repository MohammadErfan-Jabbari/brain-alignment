---
title: "Session Log: 2026-06-09 00:13 — Scope lock, git, and commit norm"
tags: [timeline]
---

# Session Log: 2026-06-09 00:13 — Scope lock, git, and commit norm

**Project:** brain-alignment (MSc thesis, UC3M)
**Phase:** Map → Claim boundary (reframed to feasibility-first)

---

## Purpose

Close out the bootstrap session by locking scope with Erfan, initializing git, and adopting a
continuous-atomic-commit norm — then leave a clean handoff so the next (fresh) session can start work
overnight on the benchmark survey.

## What happened

- **Scope confirmed with Erfan** and written through the docs:
  - This is the **MSc thesis (ML for Health, UC3M)** — the NeurIPS track is dropped. Erfan is a
    pre-PhD student finishing the master.
  - **Deadline ~end of August 2026** (tentative, exact TBD).
  - **The "edge"/deployment framing is deferred.** The thesis must first answer the *fundamental*
    question: does the LLM↔brain alignment signal carry usable, confound-free information that helps
    a downstream task? Method/deployment come only after that holds.
- Reframed [`00-charter.md`](../00-charter.md) around "The first question (gates everything)"; promoted the language-fMRI
  benchmark survey to the active task in [`tasks.md`](../tasks.md).
- **Git initialized** on `main` earlier; 5 atomic scoped commits for the bootstrap.
- **Adopted continuous-atomic-commit norm** — clarified that git is a *complement* to, not a
  replacement for, the docs decision log + timeline (docs = why, git = what/when).

## Decisions made

- **D006** — Scope lock: MSc/UC3M, ~end-Aug-2026, feasibility-first, edge deferred.
- **D007** — Continuous atomic commits as the granular mechanical history, alongside the docs record.

## Current truth

The premise is not yet proven, only well-motivated. Job #1 is to find a language-fMRI benchmark with
adequate statistical power and an open license — without it there is nothing to measure, and the
thesis would need reframing. That survey gates the toy pilot, which in turn is where the feasibility
question gets answered.

## Next session

- [ ] Run the language-fMRI benchmark survey + power analysis (`lit-scout`), write
      [`docs/04-data-benchmarks.md`](../04-data-benchmarks.md) with a recommendation. Commit atomically per step.
- [ ] Then: decide the form of $\mathcal{L}_{\text{brain}}$; scaffold the toy pilot.

## Friction & improvements

- Clarified (Erfan's correction) that atomic git commits are the *mechanical* history; the docs
  decision log and timeline remain the canonical *research* record and must always be maintained.
  Wording in D007 and `CLAUDE.md` tightened accordingly.


## Related
- [`ladder.md`](../ladder.md) — the canonical status board
- [`map.md`](../map.md) — code system (Q/E/A/D/L) & journey map
