% Supervisor submission derivative of ../rewrite (D064/D065).
% Local section copies allow submission-specific review and condensation.
% ../rewrite remains authoritative; accepted scientific changes must be synchronized there.
% Every retained load-bearing result keeps its \evd{Ennn} marker and numbers.tex key.
% Build from this directory: latexmk -pdf main-submission.tex

% ---- cover-page fields (CONFIRM with Erfan before sending) ----------------
\newcommand{\DegreeName}{Machine Learning for Health}        % confirmed by Erfan, 2026-09-01
\newcommand{\AcademicYear}{2025-2026}                        % confirmed by Erfan, 2026-09-01
\newcommand{\ThesisTitle}{Evaluating Brain Alignment as a Training Signal for Language Models}
\newcommand{\AuthorName}{MohammadErfan Jabbari}
\newcommand{\FirstTutor}{Alejandro Lancho Serrano}           % TODO confirm tutor ordering
\newcommand{\SecondTutor}{Pablo Mart\'inez Olmos}
\newcommand{\ExternalSupervisorA}{Marco Fiore}              % IMDEA Networks; spelling verified against networks.imdea.org
\newcommand{\ExternalSupervisorB}{Claudio Fiandrino}        % IMDEA Networks; spelling verified against networks.imdea.org
\newcommand{\PlaceAndDate}{Legan\'es, September 2026}                    % Escuela Politecnica Superior, Leganes campus (Erfan, 2026-09-01)
% ---------------------------------------------------------------------------

\PassOptionsToPackage{hidelinks}{hyperref} % neurips_2025 and preamble both load hyperref
\documentclass{article}
\usepackage[final,nonatbib]{style/neurips_2025} % official NeurIPS 2025 style; nonatbib: we keep biblatex
\makeatletter\renewcommand{\@noticestring}{}\makeatother % thesis, not a NeurIPS track: no notice string (final alone leaves \@trackname undefined)
\usepackage{preamble} % vendored copy of ../rewrite/preamble.sty
\usepackage{pdfpages} % page 1 is the official UC3M template's own output
\addbibresource{references.bib} % single source of references
\input{../rewrite/numbers}   % single source of keyed values
\input{../rewrite/acronyms}  % single source of acronym registry

\title{\ThesisTitle}
\author{\AuthorName}
\date{\today}

\begin{document}

% ========================= UC3M official cover page ========================
% Page 1 is produced by the official UC3M TFM Overleaf template itself, not
% reproduced here: cover/cover.tex holds memoria.tex's own titlepage block and
% its own document settings (report 12pt, geometry hmargin 3cm, txfonts), so
% the page renders in the template's fonts and metrics rather than NeurIPS's.
% Rebuild it with:  cd cover && latexmk -pdf cover.tex
\includepdf[pages=1]{cover/cover.pdf}
% ===========================================================================

\maketitle

% Submission-specific wording pass over the canonical abstract; scientific claims
% and evidence states remain unchanged.
\begin{abstract}

Language-model representations can predict recorded \ac{fmri} responses to language, but that does not show that brain responses can improve model training.
We tested whether adding recorded \ac{fmri} responses, or synthetic brain responses generated from text, to knowledge distillation gives a smaller student model an advantage that comes specifically from brain-response information.
We first measured how well model representations predicted held-out \ac{fmri} responses, beyond nuisance features and beyond architecture-matched untrained models, using two assays: one over brain regions responding to isolated sentences, one over voxels responding to a spoken story.
Both of these assays supported brain prediction under those controls.
We then compared training on correctly paired responses against training on permuted responses, which carry the same values but lose their pairing with the sentence that produced them.
For synthetic responses we asked three further questions: whether training made the synthetic target easier to recover from the student, whether the resulting changes remained after removing the prediction head used only during training, and whether the student improved on independently recorded \ac{fmri} responses beyond ordinary \ac{kd}.
We also compared the synthetic-response arm against a text-derived control, since only such a comparison can attribute a difference to brain-response content rather than to any similarly structured target.
A separate assay tested one narrow pathway alone: whether a 50-component linear projection of the synthetic target improved held-out \ac{fmri} prediction beyond nuisance features and beyond a control that keeps the same target values but breaks their pairing with the sentences.
For a GPT-2 student trained from scratch, the gap between teacher and student, which is the room an intervention could close, was confounded by language quality.
At comparable language quality that gap remained unresolved for a pretrained GPT-2 student, and we did not measure it for the Qwen student.
Recorded-response training did not reliably improve participant-level brain prediction over permuted-response training.
Synthetic-response training did make the target easier to recover, and it did change the student retained after the head was removed, while staying inside the language-quality tolerance fixed in advance.
The projected target, however, did not clear the improvement threshold set beforehand.
The text-derived control was too poorly matched to show whether any difference was specific to brain-response information, and direct participant-level transfer over ordinary \ac{kd} was not demonstrated.
Mean scores on the practical endpoint, held-out language-modeling quality on text from outside the training corpus, favored recorded-response training.
We did not report uncertainty for that paired comparison, however, and the brain-prediction improvement it depends on was unstable across random seeds, so brain-specific utility was not demonstrated.
The routes we tested therefore did not establish a reliable brain-specific training advantage, and they do not show that brain-response-guided training is ineffective in general.
The contribution is the separation of claims: for each route, the thesis says which of measurement, manipulation, direct transfer, attribution, and utility the evidence actually supports.

\end{abstract}
\acresetall

% ---------------------------------------------------------------------------
% MAIN TEXT: local copy of the canonical rewrite for submission-specific review.
% Accepted scientific changes must later be synchronized to ../rewrite/.
% ---------------------------------------------------------------------------

\input{sections/01_introduction}
\input{sections/02_valid_training_claim}
\FloatBarrier
\input{sections/03_experimental_framework}
\FloatBarrier
\input{sections/04_results}
\input{sections/05_discussion}
\input{sections/06_limitations}
\input{sections/07_conclusion}

\appendix
\input{sections/A_evidence_provenance}
\input{sections/B_data_targets_preprocessing}
\input{sections/C_training_architecture}
\FloatBarrier
\input{sections/D_estimators_inference}
\input{sections/E_secondary_experiments}
\input{sections/F_sensitivities_tables}

\printbibliography
\end{document}
