% Supervisor submission derivative of ../rewrite (D064/D065).
% Local section copies allow submission-specific review and condensation.
% ../rewrite remains authoritative; accepted scientific changes must be synchronized there.
% Every retained load-bearing result keeps its \evd{Ennn} marker and numbers.tex key.
% Build from this directory: latexmk -pdf main-submission.tex

% ---- cover-page fields (CONFIRM with Erfan before sending) ----------------
\newcommand{\DegreeName}{Machine Learning for Health}        % confirmed by Erfan, 2026-09-01
\newcommand{\AcademicYear}{2025-2026}                        % confirmed by Erfan, 2026-09-01
\newcommand{\ThesisTitle}{Evaluating Brain Alignment as a Training Signal for Language Models}
\newcommand{\AuthorName}{MohammadErfan Jabbari}
\newcommand{\FirstTutor}{Alejandro Lancho Serrano}           % TODO confirm tutor ordering
\newcommand{\SecondTutor}{Pablo Mart\'inez Olmos}
\newcommand{\ExternalSupervisorA}{Marco Fiore}              % IMDEA Networks; spelling verified against networks.imdea.org
\newcommand{\ExternalSupervisorB}{Claudio Fiandrino}        % IMDEA Networks; spelling verified against networks.imdea.org
\newcommand{\PlaceAndDate}{Legan\'es, September 2026}                    % Escuela Politecnica Superior, Leganes campus (Erfan, 2026-09-01)
% ---------------------------------------------------------------------------

\PassOptionsToPackage{section}{placeins} % contain floats within their section
\PassOptionsToPackage{hidelinks}{hyperref} % neurips_2025 and preamble both load hyperref
\documentclass{article}
\usepackage[final,nonatbib]{style/neurips_2025} % official NeurIPS 2025 style; nonatbib: we keep biblatex
\makeatletter\renewcommand{\@noticestring}{}\makeatother % thesis, not a NeurIPS track: no notice string (final alone leaves \@trackname undefined)
\usepackage{preamble} % vendored copy of ../rewrite/preamble.sty
\usepackage{pdfpages} % page 1 is the official UC3M template's own output
\addbibresource{references.bib} % single source of references

% ---- float placement -------------------------------------------------------
% Floats are [tbp], not [H]. An [H] float cannot move, so a tall one that does not
% fit ends the page early; NeurIPS sets \flushbottom, which then injects the slack
% into mid-page gaps instead of leaving it at the foot. Measured cost: 4 pages.
\raggedbottom                        % collect slack at the page foot, not mid-page
\renewcommand{\topfraction}{0.9}     % a float may own 90% of a page top (default .7)
\renewcommand{\bottomfraction}{0.9}
\renewcommand{\textfraction}{0.07}   % a page needs only 7% text (default .2)
\renewcommand{\floatpagefraction}{0.85} % a float page must be 75% full (default .5)
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{4}
% -----------------------------------------------------------------------------
\input{../rewrite/numbers}   % single source of keyed values
\input{../rewrite/acronyms}  % single source of acronym registry

\title{\ThesisTitle}
\author{%
  \AuthorName\\
  Universidad Carlos III de Madrid\\
  IMDEA Networks Institute\\
  Master's Degree in \DegreeName\\
  \texttt{mohammaderfan.jabbari@networks.imdea.org}%
}
\date{}

\begin{document}

% ========================= UC3M official cover page ========================
% Page 1 is produced by the official UC3M TFM Overleaf template itself, not
% reproduced here: cover/cover.tex holds memoria.tex's own titlepage block and
% its own document settings (report 12pt, geometry hmargin 3cm, txfonts), so
% the page renders in the template's fonts and metrics rather than NeurIPS's.
% Rebuild it with:  cd cover && latexmk -pdf cover.tex
\includepdf[pages=1]{cover/cover.pdf}
% ===========================================================================

\maketitle

% Submission-specific wording pass over the canonical abstract; scientific claims
% and evidence states remain unchanged.
\begin{abstract}
\parfillskip=0pt plus 0.65\linewidth % the abstract is narrower than \textwidth
Language-model representations can predict human brain responses to language recorded with \ac{fmri}.
That is a measurement result, and it does not show these responses can improve how a model is trained.
This thesis tests the training claim directly.
Does adding brain responses as an auxiliary objective alongside \ac{kd} give a smaller student model an advantage that comes specifically from brain-response content?
Those responses are either recorded from people or synthesized from text by a model trained on recorded responses.
The question resolves into five claims that this thesis keeps apart.
\emph{Measurement} is predicting recorded responses from a frozen model, \emph{manipulation} is changing the student with a brain-response objective, \emph{attribution} is tracing that change to brain-response content rather than to any dense target, \emph{transfer} is improving prediction of independently recorded responses, and \emph{utility} is improving a practical endpoint.
Each claim needs its own comparator, and none establishes another.
Measurement holds.
On an isolated-sentence dataset and a spoken-story dataset from one deeply sampled participant, frozen representations predicted held-out responses beyond simple stimulus properties, and in the story data beyond an architecture-matched untrained model.
The recorded-response route stops at attribution.
Against a control trained on the same responses permuted across sentences, correct pairing gave no reliable advantage across nine participants, the inference unit this claim requires.
Mean scores on a downstream language endpoint did favor the recorded responses, but the predictivity change they were meant to pay off was unstable across seeds.
The synthetic-response route passes manipulation and stops immediately after.
The student learned the target and stayed changed after the training-only components were removed, at language quality inside a tolerance set in advance.
Transfer beyond ordinary \ac{kd} was not demonstrated at the participant level, and the text-derived control was too poorly matched to attribute any difference to brain-response content.
The measurement assays and manipulation checks are themselves positive, so these negative findings are not a failed pipeline.
No end-to-end positive control was available, however, so the sensitivity of the full intervention-to-transfer path remains unestablished.
Neither route establishes brain alignment as a usable training signal, and neither rules out a benefit from other targets, interventions, or populations.
The contribution is the separation that keeps a valid positive result at one stage from being read as support for the training claim.
\end{abstract}
\acresetall

\clearpage
\setcounter{tocdepth}{2}
\tableofcontents
\clearpage

% ---------------------------------------------------------------------------
% MAIN TEXT: local copy of the canonical rewrite for submission-specific review.
% Accepted scientific changes must later be synchronized to ../rewrite/.
% ---------------------------------------------------------------------------

\input{sections/01_introduction}
\input{sections/02_valid_training_claim}
\input{sections/03_experimental_framework}
\input{sections/04_results}
\input{sections/05_discussion}
\input{sections/06_limitations}
\input{sections/07_conclusion}

\appendix
\input{sections/A_evidence_provenance}
\input{sections/B_data_targets_preprocessing}
\input{sections/C_training_architecture}
\input{sections/D_estimators_inference}
\input{sections/E_secondary_experiments}
\input{sections/F_sensitivities_tables}

\section*{Data and code availability}
\addcontentsline{toc}{section}{Data and code availability}
The Tuckute condition-B responses \autocite{tuckute2024}, the LeBel story-listening responses \autocite{lebel2023}, the Pereira sentence set \autocite{pereira2018}, WikiText-103 \autocite{merity2017}, and TRIBE~v2 \autocite{dascoli2026} are publicly available from their original sources.
The analysis code, the numbered experiment records E001 to E030, and the source of this manuscript are available at \url{https://github.com/MohammadErfan-Jabbari/brain-alignment}.
Appendix~\ref{app:training} states the reproduction scope and the limits on exact replay.

\printbibliography[heading=bibintoc]
\end{document}
