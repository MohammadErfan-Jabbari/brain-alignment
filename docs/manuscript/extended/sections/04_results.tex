\section{Results}

The experiments test a sequence of increasingly demanding claims.
First, an alignment score must detect information that language training adds to a model representation.
Second, ordinary distillation must leave enough alignment headroom for an additional objective to matter.
Third, an alignment-guided objective must move held-out alignment beyond an appropriate control.
Fourth, that movement must generalize across participants or another biological unit.
Finally, it must improve an outcome that was not optimized during training.

The measurements in this sequence do not share an inference unit.
For compactness, let $A$ denote held-out nuisance-subtracted unique $R^2$, and let $u$, $s$, and $k$ index participant, training seed, and held-out stimulus block.
Table~\ref{tab:results-map} states the load-bearing estimand and the order of aggregation for each claim.

\begin{table}[H]
  \centering
  \footnotesize
  \caption{Estimand map for the experimental chain. Each estimator aggregates repeated technical measurements before using the unit and scope named in the third column.}
  \label{tab:results-map}
  \begin{tabular}{@{}p{0.18\textwidth}p{0.36\textwidth}p{0.17\textwidth}p{0.21\textwidth}@{}} % \evd{E002}
    \toprule
    \raggedright Claim & \raggedright Estimand and estimator & \raggedright Unit and inferential scope & \raggedright Result \tabularnewline
    \midrule
    \raggedright Controlled measurement & \raggedright Mean held-out $A(\mathrm{trained})-A(\mathrm{untrained})$ across fixed contiguous blocks. & \raggedright Held-out blocks within each fixed target; fitting folds share training items, and no population inference is made. & \raggedright Positive on both recorded-fMRI substrates \evd{E002}\evd{E006}. \tabularnewline
    \raggedright Recorded-target intervention & \raggedright Average $A(\mathrm{real})-A(\mathrm{control})$ over seeds and folds within each participant, then average participants. & \raggedright Nine participants after seed and fold aggregation. & \raggedright No supported participant-level benefit in the tested regimes \evd{E008}\evd{E011}\evd{E013}. \tabularnewline
    \raggedright Dense-proxy uptake & \raggedright Seed-paired target-$R^2$ gain over KD, plus parameter and representation change from KD. & \raggedright Six technical training seeds for one fixed procedure; no biological or target-population inference. & \raggedright Target fit and student movement pass \evd{E016}\evd{E025}. \tabularnewline
    \raggedright Comparator identification & \raggedright Frozen conjunction over target geometry, baseline predictability, and seed-paired student movement. & \raggedright Two deterministic item samples for geometry and six technical seeds for movement; no population inference. & \raggedright Saved text-feature comparator fails measured-axis comparability \evd{E026}. \tabularnewline
    \raggedright Biological transfer & \raggedright Average TRIBE-minus-KD within each participant after seed and fold aggregation; retain the preregistered difference-in-differences as a separate relative comparison. & \raggedright Nine participants after seed and fold aggregation. & \raggedright Direct mean is near zero; the relative upper bound is below the continuation threshold but cannot identify target content after E026 \evd{E025}\evd{E026}. \tabularnewline
    \raggedright External utility & \raggedright Out-of-domain or sample-efficiency contrast, conditional on a verified intervention or informative privileged signal. & \raggedright Eight seeds for E009; paragraph groups for the E024 precondition. & \raggedright No payoff claim is licensed \evd{E009}\evd{E024}. \tabularnewline
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Controlled measurability on real fMRI}
\label{sec:results-measure}

The first question is whether a trained language model contains brain-predictive information beyond the recorded low-level properties of the stimulus.
E002 measures this on the Tuckute sentence benchmark by fitting ridge encoding models from a fixed LM layer to five participant-averaged left-hemisphere language ROIs.
The nuisance-only model contains sentence length, position, and static-embedding features, and the full model adds the contextual LM representation at matched PCA capacity.
The estimand is the held-out semipartial increment, or unique $R^2$, under five contiguous stimulus folds.
A same-architecture untrained network controls for feature dimension and architecture without language training \evd{E002}.

All three trained models have positive unique $R^2$, whereas every untrained seed is negative (Table~\ref{tab:roi}).
The trained-minus-untrained gaps are $+0.030$ for GPT-2, $+0.033$ for GPT-2-medium, and $+0.050$ for Qwen2.5-0.5B \evd{E002}.
The corrected mean ceiling coefficient for the five functional ROIs is $0.491$ \evd{E002}\evd{E004}.
Because that coefficient and unique $R^2$ are not the same statistical quantity, their legacy ratio is not interpreted as a fraction of explainable variance.
These estimates belong to one participant-averaged target evaluated over five stimulus folds; they establish conditional measurability on this benchmark, not population prevalence.

\begin{table}[H]
  \centering
  \footnotesize
  \caption{Tuckute ROI benchmark under five contiguous folds. Unique $R^2$ is the held-out semipartial increment over the nuisance set \evd{E002}.}
  \label{tab:roi}
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{lccccc}
    \toprule
    Model & Layer & Trained \uniqueR{} (mean$\pm$SD) & Untrained \uniqueR{} & Gap & Source \\
    \midrule
    GPT-2 & L7 & $+0.020\pm0.008$ & $-0.011$ & $+0.030$ & \evd{E002} \\
    GPT-2-medium & L14 & $+0.019\pm0.005$ & $-0.014$ & $+0.033$ & \evd{E002} \\
    Qwen2.5-0.5B & L12 & $+0.036\pm0.010$ & $-0.014$ & $+0.050$ & \evd{E002} \\
    \bottomrule
  \end{tabular}}
\end{table}

E006 asks whether the same trained--untrained contrast survives at voxel resolution in naturalistic data.
It uses the 20-story LeBel data for participant UTS03 and selects \result{e006_reliable_voxels} voxels using split-half reliability from a separate repeated story.
The encoding model holds out entire stories and subtracts word and phoneme rate, duration and length, log frequency, and static-embedding nuisance features.
The primary estimand is again the trained-minus-untrained unique-$R^2$ gap on the fixed UTS03 endpoint \evd{E006}.

The gap is \result{e006_gpt2_gap} for GPT-2 and \result{e006_qwen_gap} for Qwen2.5-0.5B.
Ninety-five percent and 99\% of selected voxels, respectively, have positive pointwise gaps, and each model has a positive mean gap on all five held-out story blocks \evd{E006}.
The untrained unique $R^2$ is $-0.017$ in both model comparisons \evd{E006}.
These spatial and story-block signs show that the result is not confined to a small voxel set or one held-out story.
They are not independent replicates: all voxels come from one participant, voxels are spatially dependent, and the story folds share training stories.
The original voxel-bootstrap confidence intervals and the E006-derived E007 minimum-detectable-effect claim are therefore retracted \evd{E006}.

\begin{table}[H]
  \centering
  \footnotesize
  \caption{Conditional voxelwise result on LeBel UTS03. Spatial prevalence and story-block signs are sensitivity checks within one participant, not population inference \evd{E006}.}
  \label{tab:vox}
  \begin{tabular}{lcccc}
    \toprule
    Model & Layer & Trained$-$untrained gap & Positive selected voxels & Positive story blocks \\
    \midrule
    GPT-2 & L7 & \result{e006_gpt2_gap} & 95\% & 5/5 \\
    Qwen2.5-0.5B & L12 & \result{e006_qwen_gap} & 99\% & 5/5 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \resizebox{0.82\textwidth}{!}{\input{../figures/fig04_reality_gap.tikz}}
  \caption{Mean unique $R^2$ on the selected LeBel UTS03 voxels. Arrows show the trained-minus-untrained gaps, and labels report spatial prevalence within UTS03 \evd{E006}.}
  \label{fig:a2}
\end{figure}

E002 and E006 therefore establish the first gate: language training adds linearly accessible information that predicts held-out fMRI beyond the recorded nuisance set.
They do not show that the information can be increased by training, that it generalizes across participants, or that increasing it would improve language modeling.
The two datasets contribute different parts of that statement.
Tuckute shows the effect on isolated sentences and a compact participant-averaged language-network target across three model families, while LeBel shows it on continuous stories with temporal controls and a dense response from one deeply sampled participant.
The common trained-over-untrained direction makes the measurement premise less dependent on a single stimulus format.
The different biological units prevent their combination into one population effect, so they are treated as complementary existence evidence rather than a meta-analysis.

\subsection{Distillation leaves headroom, but alignment is coupled to model quality}
\label{sec:results-preservation}

E003 asks whether ordinary logit knowledge distillation preserves the teacher's alignment without an explicit brain objective.
The load-bearing arm initializes a GPT-2 student randomly and trains it from the logits of GPT-2-medium.
Warm-start KD, ordinary language-model fine-tuning, pretrained GPT-2, distilgpt2, and an untrained network provide reference points.
All arms are scored at fixed layers on the same participant-averaged Tuckute target and nuisance-controlled contiguous folds \evd{E003}.

The cold-init student ends \result{e003_kd_cold_delta} below its teacher in unique $R^2$ ($p<0.001$ under the recorded bootstrap) and retains \result{e003_kd_cold_retention} of the teacher--floor interval \evd{E003}.
This rules out preservation for free in the tested from-scratch run and creates measurable headroom.
The magnitude is not an objective-specific estimate because the cold student remains under-trained: held-out perplexity is $477$, compared with $74$ for its teacher and $44$--$95$ for the converged warm arms.
Absolute unique-$R^2$ values also vary with PCA rank, although the retention ordering survives $n_{\mathrm{PCA}}\in\{25,50,100\}$ \evd{E003}.

\begin{table}[H]
  \centering
  \footnotesize
  \caption{Distillation and reference arms on the participant-averaged Tuckute benchmark \evd{E003}. Floor-anchored retention is $\rho'=(A_S-A_0)/(A_T-A_0)$; distilgpt2 uses GPT-2 as its lineage teacher.}
  \label{tab:kd}
  \resizebox{\textwidth}{!}{%
  \begin{tabular}{llcccc}
    \toprule
    Arm & Initialization/objective & \uniqueR{} & $\rho'$ [bootstrap CI] & Held-out ppl & Interpretation \\
    \midrule
    GPT-2-medium & pretrained teacher & $+0.0188$ & --- & $74$ & teacher reference \evd{E003} \\
    GPT-2 & pretrained & $+0.0195$ & $1.02$ [$0.77,1.27$] & $105$ & inherited alignment \evd{E003} \\
    LMFT-warm & warm, language loss & $+0.0174$ & $0.95$ [$0.81,1.13$] & $44$ & drift control \evd{E003} \\
    KD-warm & warm, logit KD & $+0.0144$ & $0.84$ [$0.67,1.04$] & $95$ & drift under KD \evd{E003} \\
    distilgpt2 & released distilled model & $+0.0076$ & $0.60$ [$0.42,0.84$] & $169$ & lineage reference \evd{E003} \\
    KD-cold & random, logit KD & $+0.0005$ & \result{e003_kd_cold_retention} & $477$ & under-trained headroom \\
    untrained & random floor & $-0.0102$ & $0$ & $\sim6\times10^4$ & floor \evd{E003} \\
    \bottomrule
  \end{tabular}}
\end{table}

The same run exposes the quality confound.
Across its five non-teacher trained/reference arms, alignment correlates with log perplexity at Pearson \result{e003_quality_corr} \evd{E003}.
That correlation uses few heterogeneous points and is not a general law, so E015 repeats the analysis with a tokenizer-independent quality measure across 22 models from six decoder families.
Its primary full-range association between mid-layer unique $R^2$ and log bits per byte is $r=-0.783$, with family-cluster bootstrap 95\% interval $[-0.911,-0.500]$ \evd{E015}.
Leave-one-family-out estimates remain between $-0.738$ and $-0.833$ \evd{E015}.
The 22 models are not 22 independent scientific replicates: the six families form approximately two model generations, and models within scaling families share training recipes.

The coupling weakens where current brain-tuning comparisons operate.
For models with bits per byte below $1.30$, $r=-0.501$ with Fisher interval $[-0.859,+0.188]$; in the overlap band $[1.13,1.26]$, $r=-0.480$ with interval $[-0.885,+0.340]$ \evd{E015}.
Those capable-band intervals include zero.
A scale-adjusted partial correlation remains negative at $-0.675$ with Fisher interval $[-0.857,-0.344]$, but bits per byte and parameter count are highly collinear, so this does not isolate a pure quality mechanism \evd{E015}.
Family residuals are also not identified because the families lack common quality support; the recorded ANCOVA family test has $p=0.204$ \evd{E015}.

\begin{figure}[t]
  \centering
  \safeimage[width=0.82\textwidth]{../figures/fig06_quality_alignment.png}
  \caption{Tokenizer-independent language-model quality and controlled mid-layer alignment across the 22 E015 models. The fitted line summarizes the full range; the shaded region marks the capable/overlap range in which the association is weaker and individually uncertain. Points share family and generation structure, so they are not 22 independent scientific replications \evd{E015}.}
  \label{fig:quality-alignment}
\end{figure}

\begin{figure}[t]
  \centering
  \resizebox{0.98\textwidth}{!}{\input{../figures/fig05_kd_retention.tikz}}
  \caption{Plain KD leaves alignment headroom, but alignment co-varies with language-model quality in the E003 reference set. The line and coefficient describe the five non-teacher E003 points, not the corrected cross-family E015 law \evd{E003}.}
  \label{fig:kd}
\end{figure}

The combined conclusion is narrower than either ``KD destroys brain information'' or ``alignment is just perplexity.''
Ordinary KD can leave a student below its teacher, but E003 does not distinguish objective-specific shedding from under-training and quality.
E015 shows that quality is a necessary matching variable across a broad modern-decoder comparison, while its capable-band and architecture-residual analyses show that output quality does not determine the hidden representation.
The intervention experiments therefore compare real brain targets with matched-regime or matched-perplexity controls rather than a vanilla model alone.
This distinction also changes the interpretation of ``headroom.''
The teacher--student gap is an opportunity only if some of it is caused by an internal choice that an auxiliary objective can alter without paying the same cost in perplexity.
E003 establishes the gap but not that counterfactual.
E015 makes unequal quality a credible alternative explanation, yet its capable-band uncertainty leaves room for representations of similar output quality to differ in alignment.
The intervention stage is needed precisely because neither preservation nor cross-model correlation identifies the movable component.

\subsection{The apparent intervention gain depends on the inference target}
\label{sec:results-inference}

E004 is the first direct lever test.
It co-trains a Qwen2.5-0.5B student with an MSE brain term and compares it with an otherwise identical block-permuted-target twin \evd{E004}.
Stimulus folds, optimization budget, nuisance model, student, teacher, and brain-loss weight are held fixed.
The estimand is the real-minus-permuted change in held-out unique $R^2$ relative to the same untuned base \evd{E004}.

The point estimate is \result{e004_qwen_specific}.
The original interval treated three seeds by five folds as 15 independent observations and excluded zero.
After averaging seeds within fold, the valid five-fold $t$ interval is \result{e004_qwen_fold_ci} \evd{E004}.
Fold 4 supplies 64\% of the summed contrast, and removing it reduces the estimate to \result{e004_qwen_lofo}.
The run's own unpaired permutation exceedance flag is false.
E004 therefore provides no inference-unit-valid evidence of a held-out lever, while its five folds cannot exclude a small effect near the observed mean.

E005 asks the related matched-perplexity question against the five-participant-averaged target.
At $\lambda=10$, its real-minus-permuted mean is \result{e005_apparent}.
The flat seed-by-fold bootstrap interval $[+0.0023,+0.0171]$ is pseudo-replicated; the five-fold $t$ interval is $[-0.0030,+0.0193]$, the median is $+0.0034$, and one fold supplies 52\% of the mean \evd{E005}.
Four of five fold means are positive, so the result is a small averaged-target trend rather than a precise null.

The later $\lambda$ sweep separates a near-rate improvement from a rate--distortion trade.
At $\lambda=1$, the real-minus-matched-permuted contrast is $+0.0030$ with fold interval $[-0.0025,+0.0085]$ \evd{E005}.
At $\lambda=3$, it is $+0.0063$ with interval $[-0.0092,+0.0218]$, and at $\lambda=10$ it is $+0.0072$ with interval $[-0.0044,+0.0188]$ \evd{E005}.
At $\lambda=30$, the contrast is $+0.0060$ $[+0.0002,+0.0119]$, but held-out perplexity worsens from approximately $51.5$ for KD-only to $67.4$ \evd{E005}.
The only fold-level-positive arm therefore buys alignment by moving to a worse language-model operating point, not by providing a free matched-rate improvement.

E008 changes the biological inference unit.
It repeats the real-versus-permuted comparison separately for nine participants, using three optimization seeds and five shared folds within each participant.
The estimand is the across-participant mean of each participant's seed-and-fold-aggregated real-minus-permuted effect.
The mean is \result{e008_mean}, the nine-participant $t$ interval is \result{e008_ci}, and 5/9 participant effects are positive (two-sided sign $p=0.50$) \evd{E008}.
The fold-clustered sensitivity estimate is \result{e008_fold_mean} with interval \result{e008_fold_ci}, and every leave-one-fold-out analysis fails to establish a positive effect.
The observed participant variance gives an 80\%-power minimum detectable effect of approximately $+0.0006$ to $+0.0013$, well below the averaged-target estimate \evd{E008}.

The four participants used to construct the earlier averaged target have an individual mean of $-0.00010$ \evd{E008}.
The five held-out participants have mean $+0.00026$, also with an interval containing zero \evd{E008}.
Thus, the E005 estimate is not an estimate of a mean per-person effect.
It is an effect on a different, higher-SNR target: the response shared across the averaged participants.

E010 tests whether participant count alone causes a smooth increase in the intervention gap.
Its initial nested subsets give gaps of $-0.0002$, $-0.0001$, $+0.0023$, $+0.0194$, and $+0.0071$ for $k=1,2,3,5,9$ averaged participants \evd{E010}.
Because subject identity changes with $k$, this design confounds count with which participants enter the average.
The random-subset rerun gives $+0.0015$, $-0.0013$, $+0.0088$, and $+0.0094$ for $k=1,3,5,9$, with standard deviations between $0.005$ and $0.015$ \evd{E010}.
The non-monotone, noisy rerun does not identify a causal dose-response law.
It remains suggestive corroboration that averaged and individual targets behave differently.

E014 isolates the measurement effect without training an LM.
Using the same Qwen representation, it compares encoding of individual targets with encoding of random participant averages.
Mean per-participant unique $R^2$ is $+0.0069$, with 7/9 participants positive, whereas the standard five-participant average scores $+0.0357$ \evd{E014}.
The count sequence for $k=1,2,3,5,9$ is $+0.0039,+0.0162,+0.0324,+0.0444,+0.0702$ \evd{E014}.
Because individual scores are already positive, this is not evidence that averaging fabricates an otherwise absent encoding signal.
It shows legitimate SNR improvement and a change from individual-response alignment to alignment with the shared response component.

Taken together, E004 and E005 provide small averaged-target trends but no reliable near-rate lever at five-fold inference.
E008 supplies the load-bearing biological result: no mean per-individual gain in nine participants under the tested matched-regime objective.
E010 does not convert the averaged-versus-individual contrast into a clean causal dose response, and E014 shows why a larger score on an averaged target can still be a legitimate measurement of a different estimand.
The apparent contradiction is therefore resolved at the level of the question being estimated.
E005 asks whether training helps predict one denoised group-average response, and its small positive trend is compatible with an intervention that follows highly shared stimulus structure.
E008 asks whether the same real-versus-control advantage is positive on average across individual people, and it finds a tightly bounded mean near zero for the predeclared effect size.
Neither result invalidates the other measurement, but only the latter supports inference to participant variation.
This is why response averaging is described as an estimand change rather than as a universally invalid artifact.

\subsection{Alternative intervention regimes do not rescue the tested lever}
\label{sec:results-alternatives}

E011 asks whether the E008 result is caused by insufficient low-rank adaptation (LoRA) capacity.
It increases adapter rank from 16 to 64 and doubles the training duration while holding the target, objective, participant set, and matched-regime control fixed.
Across the same nine participant units, the mean real-minus-permuted effect is \result{e011_mean} with $t$ interval \result{e011_ci} \evd{E011}.
Six of nine effects are positive, but one participant contributes 79\% of their sum; leaving that participant out returns the estimate to approximately $+0.00010$ \evd{E011}.
Absolute alignment and perplexity move about as much as in E008, so this result establishes robustness to extra adapter capacity at the same effective operating point, not robustness to every stronger manipulation.

E013b changes the loss geometry from MSE to symmetric contrastive training while keeping the nine-participant ROI design.
Its mean is \result{e013b_mean} with participant $t$ interval \result{e013b_ci}, 4/9 positive signs, and a matched-perplexity intercept of approximately $-0.0003$ \evd{E013}.
The contrastive objective therefore does not rescue the ROI result.
The scope is limited by the five-dimensional ROI target, which is a weaker test of a contrastive objective than a high-dimensional voxel target.

The voxelwise E013 route tests whether the MSE distillation mechanism can first create an improving representation on naturalistic LeBel data.
Without a KD anchor, real-target tuning reduces held-out alignment below the untuned base in all six subject-by-seed runs, so the first run is a failed manipulation rather than a null effect \evd{E013}.
With a KD anchor on UTS03, the untuned base is approximately $0.0187$ unique $R^2$ \evd{E013}.
At $\lambda=1$, real-target tuning reaches $0.0183$ with real-minus-permuted gap $-0.0009$; at $\lambda=3$ and $6$, it falls to $0.0086$ and $0.0130$, with gaps $-0.0049$ and $-0.0039$ \evd{E013}.
No tested weight improves over the base or beats its permuted twin.
This is a single-participant mechanism failure for voxelwise MSE distillation, not a population null for every voxelwise objective.

E017 changes the parameterization to full fine-tuning.
An aggressive configuration produces no manipulation success in three UTS03 seeds and increases perplexity by a factor of $1.63$ \evd{E017}.
A gentler configuration preserves perplexity more closely and is then run for three seeds on each of UTS01, UTS02, and UTS03.
Across the nine subject-by-seed cells, the recorded real-minus-permuted mean is $+0.0003$, with descriptive cell-level $t$ interval $[-0.0002,+0.0008]$ and $p=0.274$ \evd{E017}.
Per-subject means are $+0.0005$, $-0.0001$, and $+0.0006$ \evd{E017}.
The valid biological unit is the participant, so the nine-cell interval is not population inference; the run is a three-participant feasibility result showing no demonstrated brain-specific gain in this configuration.

\begin{figure}[t]
  \centering
  \safeimage[width=0.88\textwidth]{../figures/fig07_intervention_forest.png}
  \caption{Comparable real-target minus target-control intervention contrasts. E004 and E005 use five stimulus folds; E008, E011, and E013b use nine participants. The rows are displayed together to show scale and uncertainty but are not pooled because their estimands and inference units differ \evd{E004,E005,E008,E011,E013}.}
  \label{fig:intervention-forest}
\end{figure}

These variants close specific explanations rather than the entire hypothesis space.
The ROI result is not rescued by more adapter capacity or a contrastive loss.
The tested voxelwise MSE mechanism fails to improve the base representation, and the tested full-fine-tuning configurations either damage language quality or remain indistinguishable from their permuted twins.
Different data, targets, objectives, or substantially larger participant cohorts remain outside these results.
The sequence nevertheless removes several immediate rescue explanations.
If the primary null were caused only by insufficient low-rank capacity, E011 should have enlarged the contrast; if squared-error geometry were uniquely unsuitable for ROI targets, E013b should have improved it; if adapters were the bottleneck, the gentle full-fine-tuning regime in E017 should have separated real and control targets.
None does so under its valid scope.
The voxelwise branch is even earlier in the causal chain because stronger weights fail the manipulation check itself.
These results therefore narrow the plausible success regime toward different data or target construction rather than a simple increase of the same optimization knob.

\subsection{No practical payoff is demonstrated when the prerequisite signal does not move}
\label{sec:results-practical}

E009 asks whether alignment-guided KD improves a non-brain outcome: out-of-domain perplexity on Pereira and LeBel text.
It compares KD-only, real-brain mean-squared error (MSE), block-permuted MSE, and a text-feature auxiliary target over eight training seeds.
The mediator that would make the downstream test interpretable is the real-minus-permuted alignment change on the in-domain averaged target \evd{E009}.

That mediator has mean \result{e009_fulcrum_mean}, median \result{e009_fulcrum_median}, and 5/8 positive seed signs.
Its measured 80\%-power minimum detectable effect is $0.023$, and seed 0 contributes $+0.063$ while the remaining seven seeds average approximately $+0.004$ \evd{E009}.
The representation fulcrum is therefore not reliable.
Every OOD-perplexity contrast also lies inside its measured minimum detectable effect: real MSE minus KD-only is $+0.009$ on Pereira and $+0.053$ on LeBel, while real MSE minus the permuted twin is $-0.019$ and $-0.068$ \evd{E009}.
E009 supports no detected brain-specific out-of-domain (OOD) perplexity payoff under this all-data, averaged-target setup.
It is a bounded result because the manipulation whose payoff was being tested did not reliably take hold, and because robustness and sample-efficiency outcomes were not run.

E024 tests whether a cognitive measurement can help through a different channel: learning using privileged information (LUPI), where the auxiliary measurement is available only during training.
The planned five-arm ZuCo intervention is gated on two preconditions: natural-reading gaze must contain information about the target label, and paragraph-disjoint splits must support a stable low-sample learning curve.
The data comprise 300 natural-reading sentences from 12 participants, but only seven source paragraphs \evd{E024}.

The gaze measurement itself is reliable: combined split-half reliability is $0.71$ \evd{E024}.
It is not informative for the target under natural reading.
A rich 35-dimensional gaze probe gives binary AUC $0.508$ with permutation $p=0.45$, and five-way relation-type balanced accuracy is $0.202$ against $0.200$ chance, $p=0.35$ \evd{E024}.
Linear, nonlinear, participant-stacked, and within-participant probes give the same null pattern.
Under task-directed reading, the relation-type probe reaches $0.295$ against $0.100$ chance, with permutation $p=0.003$ \evd{E024}.
This contrast shows that gaze contains relation information when readers are instructed to search for the relation, but that channel is task-intent leakage rather than portable natural-reading privileged information \evd{E024}.

The seven-paragraph structure independently fails the split gate.
Across paragraph-disjoint splits, test size ranges from 8 to 161 sentences and the empirical low-$n$ minimum detectable effect ranges from approximately $0.03$ to $0.08$ or becomes undefined \evd{E024}.
No stable leakage-free sample-efficiency estimand is available on this substrate.
The binding gate therefore stops the five-arm intervention before training.
This is evidence that the chosen privileged signal and dataset fail the required preconditions, not evidence that learning using privileged information is universally ineffective.
E009 and E024 fail at different locations and should not be collapsed into a single downstream null.
E009 implements downstream evaluation but lacks a reliable brain-specific mediator, so it cannot reveal what a successful alignment change would do.
E024 never reaches the intervention because the clean natural-reading privileged signal is neither label-informative nor stably evaluable under paragraph-disjoint splits.
Together they show why practical claims require a verified chain from target information to representation movement to held-out task performance.
They do not establish that every neural or cognitive signal lacks practical value.

\subsection{Separating synthetic-target learnability, control identification, and biological transfer}
\label{sec:results-synthetic}

E020 addressed a separate ceiling question by subtracting a leave-one-participant-out estimate of the shared response.
The reference was too unreliable and leakage-sensitive to estimate a general ceiling, so E020 is not part of the identified intervention chain.
Appendix~\ref{sec:e020} reports the failed instrument and the control that removed its apparent residual signal \evd{E020}.

E016 then asks a different question: whether a dense stimulus-derived brain target can be optimized at scale and transfer to measured fMRI alignment.
TRIBE v2 is a pretrained multimodal model that predicts high-resolution cortical fMRI responses for new stimuli \autocite{dascoli2026}.
E016 uses its 20,484 predicted cortical values per text stimulus as a synthetic neural target, not as newly recorded fMRI.
TRIBE and a projected teacher-hidden-state control each define their own synthetic target; KD-only and block-permuted variants provide within-target references.
The two synthetic target spaces are dimension-matched in the training harness, but E016 did not establish that they were matched in information, geometry, baseline difficulty, or optimization \evd{E016}.

Across six training seeds, TRIBE-target training improves its held-out synthetic target $R^2$ over KD-only by \result{e016_tribe_synth_gain}.
The projected text-feature target improves its own endpoint by \result{e016_textfeat_synth_gain} \evd{E016}.
The maximum relative perplexity deviation across the checked target and control arms is $0.009220$ \evd{E016}.
The supported within-target claim is therefore that TRIBE training improves prediction of its own target under nearly unchanged language quality.
The larger TRIBE gain than the sentence-local projected teacher-hidden-state gain is a numerical cross-target contrast whose content-specific interpretation requires a comparable control.

The saved students are then scored on one common Tuckute endpoint.
Synthetic training uses layer 6, while the real-brain evaluation uses layer 7 and the same five-participant/five-ROI average for every seed \evd{E016}.
On the common endpoint, the direct TRIBE-minus-KD mean is \result{e016_direct_transfer}, with five of six seed margins negative \evd{E016}.
The predeclared relative diagnostic, $[\mathrm{TRIBE}-\mathrm{KD}]-[\mathrm{textfeat}-\mathrm{KD}]$, is \result{e016_transfer_delta}; all six seed margins are negative, giving exact sign $p=0.03125$ \evd{E016}.
Because E026 later shows that the text-feature arm fails measured-axis comparability, this relative diagnostic cannot identify target content.
The corresponding difference relative to the two block-permuted gains is $-0.001005$, with five of six seed margins negative \evd{E016}.
The block permutation leaves 0--30\% of rows fixed and duplicates or omits one row in five of six seeds, so that contrast is a sensitivity analysis rather than an exact-null comparison.

The direct E016 result is a fixed-endpoint transfer failure.
The synthetic target is learnable, but its improvement does not improve alignment on this fixed real-brain diagnostic.
The inference unit is six stochastic training seeds over one endpoint that already averages five participants and five ROIs.
It supports neither participant-level nor population-level inference, and it does not show that all synthetic brain targets must fail.
This is stronger than a failure to fit the proxy because target fit succeeds in every seed.
It remains narrower than a biological ceiling because the endpoint is fixed, participant-averaged, and evaluated at a different student layer.

E026 tests the missing control-identification condition without retraining and without loading or consuming participant-outcome data \evd{E026}.
Under the frozen 55-check conjunction, \result{e026_check_split} checks fail; 24 pass and one is unresolved \evd{E026}.
The mismatch begins before intervention: mean KD-only target $R^2$ is \result{e026_kd_targets} for TRIBE and textfeat, respectively, leaving \result{e026_headroom_ratio} times as much mean headroom for TRIBE.
Across two disjoint target samples, normalized covariance-spectrum log-RMS differences are \result{e026_spectrum_rms}, and absolute stable-rank log-ratios are \result{e026_stable_rank_log_gaps}; all exceed their predeclared margins.
Low-level nuisance predictability differs by \result{e026_lowlevel_gaps} held-out $R^2$ across the two coordinate samples.
After training, all six global relative-parameter-update ratios and all six layer-6 centered relative-Frobenius movement ratios fail their margins.
This is not total non-parity: standardized global scale, most valid within-document autocorrelation checks, power-spectrum shape, and static-unique target predictability pass their margins, while four of six CKA-distance checks pass.
Initial-gradient comparability is unknown because the required values were not retained, and lag 1024 is unresolved because no valid within-document pairs exist.

The audit therefore shows that the saved text-feature arm fails the predeclared measured-axis comparability conjunction; it does not establish universal or information-theoretic incomparability.
Because textfeat is a Gaussian projection of the same GPT-2-medium teacher representation used for KD, its high KD baseline and low remaining headroom are consistent with same-teacher redundancy; this is a mechanism hypothesis rather than a causal result.
E026 does not identify which mismatch produced the cross-target gain, show that TRIBE contains unique biological information, or explain E025's transfer failure.
It removes the raw TRIBE-minus-textfeat proxy-gain difference as evidence of brain specificity while leaving E016's within-target learnability result and E025's near-zero direct TRIBE-minus-KD contrast intact.

E025 then removes the participant-averaging limitation by rescoring the same saved students separately for the nine complete Tuckute participants \evd{E025}.
Before opening the participant result, it verifies language quality, target uptake, and retained-student movement.
All three gates pass: every target-learning seed is positive, every target-trained student moves detectably from its KD initialization, and the largest direct target/KD or between-target quality difference is $0.002579$ bits per byte \evd{E025}.
The endpoint is therefore interpretable as a transfer result rather than a failed manipulation.

At fixed layer 7, the predeclared participant-first TRIBE-minus-text-feature mean is \result{e025_participant_delta}, with 95\% CI \result{e025_participant_ci} and a one-sided 95\% upper bound of \result{e025_participant_ucb} \evd{E025}.
That upper bound is below the internal $+0.002$ continuation threshold, and the observed 80\% minimum detectable effect is \result{e025_participant_mde} \evd{E025}.
The threshold governed whether to continue a positive-mechanism program; it is not a universal minimum biological effect.
Only four of nine participant estimates are positive, the median is \result{e025_participant_median}, and removing the highest-KD-alignment participant changes the mean to \result{e025_drop_highest} \evd{E025}.

After E026, the relative contrast remains a valid numerical comparison but cannot identify target content.
The direct TRIBE-minus-KD contrast is the cleaner biological transfer endpoint, but it estimates the net effect of the saved TRIBE-training arm versus KD, not brain-derived content in isolation.
Its mean is \result{e025_tribe_kd}, with 95\% CI \result{e025_tribe_kd_ci}; seven of nine participant estimates are negative \evd{E025}.
The small positive relative mean partly reflects a negative text-feature-minus-KD mean of \result{e025_textfeat_kd} rather than a positive direct TRIBE gain \evd{E025}.
Imageability adjustment remains near zero, while the broader nuisance stress test and the layer-6 sensitivity are negative \evd{E025}.

\begin{figure}[H]
  \centering
  \safeimage[width=0.98\textwidth]{../figures/fig09_identification_chain.png}
  \caption{The dense-proxy intervention separates four evidential gates. Panel A distinguishes KD baseline target fit, target-training gain, and remaining headroom. Panel B plots mean cumulative variance mass across the two fixed deterministic samples; shading spans their two values and is not a confidence interval. Panel C shows six-seed global-parameter and layer-6 centered-Frobenius movement ratios against the frozen $|\log r|\leq0.10$ band; centered-CKA comparability is mixed and reported in the text. In Panel D, dots are participants, diamonds are participant means, and horizontal bars are participant-level mean 95\% $t$ confidence intervals. The purple segment marks the internal $+0.002$ continuation threshold for the relative contrast only. The relative estimate cannot identify target content after E026, while the direct estimate remains interpretable as the net TRIBE-training-versus-KD endpoint rather than a content-specific effect \evd{E016}\evd{E025}\evd{E026}.}
  \label{fig:identification-chain}
\end{figure}

The four gates support a more specific conclusion than either ``the proxy worked'' or ``brain guidance failed.''
Proxy fit and retained-student movement pass.
The saved cross-target comparator fails identification.
The predeclared relative estimate has a one-sided 95\% upper bound below the internal continuation threshold, while the direct participant-level TRIBE-minus-KD estimate is near zero \evd{E025}\evd{E026}.
This dissociation is restricted to the saved students, fixed layer and nuisance design, nine-participant Tuckute cohort, and recorded sentence/ROI endpoint.
