\section{Results Across the Evidence Chain}
\label{sec:results}

The results apply the conceptual evidence chain in Figure~\ref{fig:evidence-chain} to the scientific roles defined in Section~\ref{sec:framework} and instantiated in Sections~\ref{sec:data-targets}--\ref{sec:estimands}.
Each subsection reports the decisive observation, its uncertainty at the declared inference unit, the resulting evidence status, and the narrow claim that status licenses.
The recorded-response and synthetic-response routes share measurement prerequisites and then proceed as separate intervention tests.
Sections~\ref{sec:results-signal}--\ref{sec:results-headroom} report the shared prerequisites, Section~\ref{sec:results-recorded} reports the recorded-response intervention, Sections~\ref{sec:results-predicted-identification}--\ref{sec:results-predicted-transfer} report the synthetic-response route, and Section~\ref{sec:results-utility} returns to recorded-response utility.
Later diagnostic results are interpreted within the status of their preceding prerequisites.
Table~\ref{tab:results-status} summarizes these role-specific outcomes using \emph{supported}, \emph{not demonstrated}, \emph{unresolved}, and \emph{non-identifying comparator}.

\begin{table}[H]
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{1.00}
  \caption{Role-specific evidence status and decisive observations.}
  \label{tab:results-status}
  \begin{tabularx}{\textwidth}{
    @{}
    >{\raggedright\arraybackslash}p{43mm}
    >{\raggedright\arraybackslash}p{31mm}
    >{\raggedright\arraybackslash}X
    @{}
  }
    \toprule
    Scientific role & Status & Decisive observation \\
    \midrule

    Controlled regional predictivity assay
    & \textbf{Supported}
    & Qwen2.5-0.5B trained-minus-untrained gap: \result{e002_qwen_gap} across five contiguous folds.\evd{E002} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Controlled naturalistic voxelwise predictivity assay
    & \textbf{Supported}
    & GPT-2 and Qwen2.5-0.5B gaps over matched untrained controls: \result{e006_gpt2_gap} and \result{e006_qwen_gap}.\evd{E006} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Quality-aware distillation-headroom analysis
    & \textbf{Supported}; cause \textbf{unresolved}
    & Student--teacher gap: \result{e003_kd_cold_delta}; \ac{lm} quality remains a competing explanation.\evd{E003}\evd{E015} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Recorded-response intervention, participant-averaged estimand
    & \textbf{Not demonstrated}
    & Paired gain: \result{e005_apparent} (five-fold interval \result{e005_fold_ci}); one fold dominates the mean.\evd{E005} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Recorded-response intervention, participant-specific estimand
    & \textbf{Not demonstrated}
    & Nine-participant mean: \result{e008_mean} (participant \(t\) interval \result{e008_ci}).\evd{E008} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Exact synthetic-target measurability assay
    & \textbf{Not demonstrated}; row specificity \textbf{unresolved}
    & Aligned-target increment: \result{e030_target_unique} (simultaneous interval \result{e030_target_unique_ci}); row-twin contrast uncertain.\evd{E030} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Synthetic-target recovery assay
    & \textbf{Supported}
    & WikiText recovery gain over seed-matched \ac{kd}: \result{e016_tribe_synth_gain} (interval \result{e016_tribe_synth_gain_ci}).\evd{E016} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Retained-student movement audit
    & \textbf{Supported}
    & All seeds pass; mean relative-Frobenius movement from \ac{kd}: \result{e025_tribe_movement_mean}, at acceptable \ac{lm} quality.\evd{E025} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Saved-student target-retention analysis
    & \textbf{Unresolved}
    & Fresh held-out Tuckute retention increment: \result{e030_retention_delta} (simultaneous interval \result{e030_retention_delta_ci}).\evd{E030} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Comparator-adequacy audit and attribution assessment
    & \textbf{Non-identifying comparator}
    & Text-derived control fails \result{e026_check_split} prespecified matching criteria.\evd{E026} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Saved-student biological-transfer assay
    & \textbf{Not demonstrated}
    & Participant-level synthetic-response-minus-\ac{kd} mean: \result{e025_tribe_kd} (\(95\%\) interval \result{e025_tribe_kd_ci}).\evd{E025} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Composed-path diagnostic
    & \textbf{Not demonstrated}
    & Synthetic-response-minus-\ac{kd} increment: \result{e030_overlap_delta} (simultaneous interval \result{e030_overlap_delta_ci}).\evd{E030} \\

    \specialrule{0.15pt}{0.35ex}{0.35ex}

    Bounded practical-utility evaluation
    & \textbf{Not demonstrated}
    & Pereira and LeBel recorded-minus-permuted ratios: \result{e009_pereira_recorded_permuted} and \result{e009_lebel_recorded_permuted}; both within measured sensitivity.\evd{E009} \\

    \bottomrule
  \end{tabularx}
  \vspace{0.4ex}

  \begin{minipage}{\textwidth}
    \scriptsize
    \emph{Status key.}
    \textbf{Supported}: the declared criterion passed.
    \textbf{Not demonstrated}: the tested contrast did not support the claim.
    \textbf{Unresolved}: the completed analysis could not distinguish the relevant alternatives.
    \textbf{Non-identifying comparator}: the available control cannot attribute an effect to brain-response content.
  \end{minipage}
\end{table}

\subsection{Controlled brain predictivity under regional and naturalistic assays}
\label{sec:results-signal}

Trained representations outperformed both the declared nuisance baseline and architecture-matched untrained controls in the controlled regional predictivity assay.
For the primary Qwen2.5-0.5B layer-12 comparison, controlled unique \(R^2\) was \result{e002_qwen_unique}, where the \(\pm\) term is the descriptive standard deviation across five contiguous stimulus folds.
The trained-minus-untrained difference was \result{e002_qwen_gap}.
Because the response target was averaged across five participants before readout fitting, the folds describe stimulus-block variation and do not support participant-level inference.
The assay therefore supports controlled regional predictivity for this fixed participant-averaged target.\evd{E002}

The controlled naturalistic voxelwise predictivity assay also favored trained representations when complete stories were held out.
For UTS03, the mean differences were \result{e006_gpt2_gap} for GPT-2 Small and \result{e006_qwen_gap} for Qwen2.5-0.5B, and all five held-out-story blocks had positive differences.
The voxels are spatially dependent, and the story folds share training stories, so neither provides independent biological replication.
This assay therefore supports a within-participant controlled-predictivity result conditional on UTS03 and the fixed naturalistic protocol.\evd{E006}

\begin{figure}[H]
  \centering
  \begin{subfigure}[t]{.495\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/controlled_predictivity_regional.tikz}}
    \caption{Controlled regional predictivity.}
    \label{fig:controlled-predictivity-regional}
  \end{subfigure}\hfill
  \begin{subfigure}[t]{.495\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/controlled_predictivity_naturalistic.tikz}}
    \caption{Controlled naturalistic voxelwise predictivity.}
    \label{fig:controlled-predictivity-naturalistic}
  \end{subfigure}
  \caption{Assay-specific trained--untrained contrasts; scales and inference units remain separate.\evd{E002}\evd{E006}}
  \label{fig:controlled-predictivity-results}
\end{figure}
\FloatBarrier

Figure~\ref{fig:controlled-predictivity-results} places the two trained-versus-untrained comparisons in separate panels.
Together, the assays show that trained \ac{lm} representations contain linearly accessible information useful for predicting held-out recorded \ac{fmri} responses beyond the specified nuisance features and architecture-matched untrained controls.
Their numerical estimates are not pooled or compared in magnitude because the response construction, target resolution, nuisance set, and inference unit differ.

Together, the assays establish the shared controlled-predictivity prerequisite within their tested settings.
The assays do not identify a shared computation between the \acp{lm} and the brain or establish population-general predictivity.
They also do not test whether using brain responses during training changes a retained student or improves language behavior.
Section~\ref{sec:results-headroom} next asks whether the teacher--student comparison leaves quality-aware distillation headroom.

\subsection{Observed distillation headroom and its association with language quality}
\label{sec:results-headroom}

Ordinary distillation leaves observed teacher-relative headroom in the tested from-scratch student.
Figure~\ref{fig:distillation-headroom-retention} places the GPT-2 teacher, floor, and student conditions on the floor-anchored retention scale defined in Equation~\ref{eq:headroom}.
At the fixed Tuckute layers, the randomly initialized GPT-2 Small logit-\ac{kd} student finishes \result{e003_kd_cold_delta} below the GPT-2 Medium teacher in controlled brain predictivity.
Relative to the architecture-matched untrained GPT-2 Small floor, the student retains \result{e003_kd_cold_retention_point} of the teacher-to-floor interval.
The recorded 95\% bootstrap interval over folds and seeds is \result{e003_kd_cold_retention_ci}.\evd{E003}
The tested training route therefore leaves observed headroom within this assay, although the comparison does not identify its source.

Language quality is a material competing explanation.
The randomly initialized student remains substantially weaker as an \ac{lm}, with perplexity \result{e003_kd_cold_ppl}, compared with \result{e003_teacher_ppl} for the teacher.\evd{E003}
The evaluated conditions also differ in initialization and training history, not only in objective.
Their ordered predictivity estimates establish a descriptive teacher--student gap, but they do not show that compression or \ac{kd} caused the gap.

The broader model comparison shows why later interventions must control language quality.
Across 22 decoder-only \acp{lm} from six families, controlled brain predictivity is negatively associated with log bits per byte, where higher values indicate poorer language quality.
Pearson \(r\) is \result{e015_full_corr}, with a family-cluster 95\% \ac{ci} of \result{e015_full_ci}.\evd{E015}
Figure~\ref{fig:distillation-headroom-quality} shows this full-range association and marks the capable-model band, defined as the \result{e015_capable_n} models with bits per byte below \result{e015_capable_bpb_max}.
Within that band, Pearson \(r\) is \result{e015_capable_corr}, with a 95\% \ac{ci} of \result{e015_capable_ci}.\evd{E015}
The point estimate remains moderately negative, but its interval includes zero.
Model family, scale, training data, and language quality remain entangled, so the association identifies language quality as a design constraint rather than a causal explanation.

\begin{figure}[H]
  \centering
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/distillation_headroom_retention.tikz}}
    \par\vspace{0.45em}
    \caption{Floor-anchored retention with 95\% fold-by-seed bootstrap intervals.}
    \label{fig:distillation-headroom-retention}
  \end{subfigure}
  \par\smallskip
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/distillation_headroom_quality.tikz}}
    \par\vspace{0.45em}
    \caption{Controlled predictivity across 22 decoder-only \acp{lm}.}
    \label{fig:distillation-headroom-quality}
  \end{subfigure}
  \caption{Observed headroom and its association with language quality.\evd{E003}\evd{E015}}
  \label{fig:distillation-headroom-quality-results}
\end{figure}
\FloatBarrier

Together, the results support observed, assay-conditional headroom between the teacher and the randomly initialized student.
They do not establish that the headroom is brain-specific or recoverable through a brain-response objective.
A valid intervention comparison must therefore test the brain-guided objective against a same-procedure control while maintaining comparable \ac{lm} quality.
Section~\ref{sec:results-recorded} applies this requirement to the recorded-response intervention.

\subsection{Recorded-response intervention at valid inference units}
\label{sec:results-recorded}

Participant-averaged recorded responses produce positive recorded-response-minus-permuted-response point estimates, but neither comparison establishes a reliable pairing benefit at the shared-fold inference unit.
In the initial loss-form screen, the mean contrast is \result{e004_qwen_specific}, with a five-fold \(t\) interval of \result{e004_qwen_fold_ci}.\evd{E004}
In the \ac{kd}-anchored paired intervention, the mean contrast is \result{e005_apparent}, with a five-fold \(t\) interval of \result{e005_fold_ci}.\evd{E005}
Both intervals include zero, and both estimates are sensitive to a dominant fold.
They therefore support positive averaged-target trends, not demonstrated pairing advantages.

These averaged-response comparisons answer a narrower question than participant-level inference.
Averaging responses before training creates one shared stimulus-locked target, so the fold-level contrast asks whether correct stimulus--response pairing helps for that fixed target.
It does not estimate the mean intervention effect across participants.
The recorded-response and permuted-response arms have perplexities \result{e005_real_ppl} and \result{e005_permuted_ppl}, respectively, showing no gross quality imbalance without establishing a formal equivalence margin.\evd{E005}

The participant-count diagnostic does not produce a stable monotone relationship once random participant subsets replace the confounded nested sequence.\evd{E010}
With the student frozen, averaging nevertheless increases controlled predictivity by reducing response noise and emphasizing the shared response component; individual responses still contain measurable signal.\evd{E014}
Averaging can therefore strengthen measurement of the shared response without creating biological replication.

The participant-specific intervention does not demonstrate a participant-general advantage at the biological inference unit.
After the technical seed and fold repetitions are aggregated within each participant as defined in Section~\ref{sec:recorded-intervention}, the mean recorded-response-minus-permuted-response effect across nine participants is \result{e008_mean}, with participant \(t\) interval \result{e008_ci}.\evd{E008}
The shared-fold summary is also centered near zero at \result{e008_fold_mean}, with five-fold \(t\) interval \result{e008_fold_ci}.\evd{E008}

The participant analysis could detect effects much smaller than the earlier estimate based on averaged responses.
The result is informative within the tested scope.
It neither proves an exactly zero effect nor extends beyond the tested target, model, cohort, objective, and readout.
The two analyses use different scales and estimate different quantities.
Figure~\ref{fig:recorded-response-inference} therefore shows them in separate panels rather than pooling them.

\begin{figure}[H]
  \centering
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/recorded_response_averaged.tikz}}
    \par\vspace{0.45em}
    \caption{Positive averaged-target trends remain uncertain at the shared-fold unit.}
    \label{fig:recorded-response-averaged}
  \end{subfigure}
  \par\smallskip
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/recorded_response_participant.tikz}}
    \par\vspace{0.45em}
    \caption{Participant-specific effects center near zero at the biological inference unit.}
    \label{fig:recorded-response-participant}
  \end{subfigure}
  \caption{Recorded-response contrasts at their valid fold and participant inference units.\evd{E004}\evd{E005}\evd{E008}}
  \label{fig:recorded-response-inference}
\end{figure}
\FloatBarrier

The tested variants address specific alternative explanations, but none provides evidence that changes the participant-general conclusion.
Table~\ref{tab:recorded-variants} separates participant contrasts from studies in which the intended manipulation was not demonstrated or the biological sample supported only an existence probe.
Objectives and schedules are documented in Appendix~\ref{app:training}, secondary-variant dispositions in Appendix~\ref{app:secondary}, and full numerical tables in Appendix~\ref{app:sensitivities}.

\begin{table}[H]
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{1.08}
  \caption{Recorded-response robustness checks and their scoped conclusions.}
  \label{tab:recorded-variants}
  \begin{tabularx}{\textwidth}{@{} >{\raggedright\arraybackslash}p{28mm} >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X @{}}
    \toprule
    Variant & Strongest valid observation & Scoped conclusion \\
    \midrule
    Higher-capacity \ac{lora}
    & Participant mean \result{e011_mean}, with \(t\) interval \result{e011_ci}.\evd{E011}
    & Additional adapter capacity at the fixed loss weight does not rescue the effect and does not produce a stronger effective manipulation. \\
    \midrule
    Contrastive objective
    & Participant mean \result{e013b_mean}, with \(t\) interval \result{e013b_ci}.\evd{E013}
    & Changing the loss does not rescue the result at five-\ac{roi} granularity. \\
    \midrule
    Naturalistic voxelwise \ac{lora}
    & The recorded-response arm does not produce the required improvement over its starting model and permuted control.\evd{E013}
    & The intended manipulation is not demonstrated; this is not a naturalistic population null. \\
    \midrule
    Full fine-tuning
    & Lower-intensity training shows no reliable paired gain in a three-participant analysis; higher-intensity training causes forgetting.\evd{E017}
    & The tested full-parameter route does not rescue the effect, and no population-general inference is supported. \\
    \bottomrule
  \end{tabularx}
\end{table}

Across the tested recorded-response interventions, the evidence does not demonstrate a participant-general benefit of correct stimulus--response pairing.
The participant-general benefit is not demonstrated for the tested targets, models, objectives, cohorts, and readouts; this is not a universal null.
The synthetic-response route is a separate intervention route.
Section~\ref{sec:results-predicted-identification} therefore begins that route by asking whether the exact synthetic target adds controlled predictivity of recorded responses.

\subsection{Exact synthetic-target measurability on recorded responses}
\label{sec:results-predicted-identification}

Within the declared evidence chain, the synthetic-response route first asks whether the PCA-50 representation of the exact TRIBE target adds held-out linear predictivity of recorded \ac{fmri} responses beyond the nuisance model.
Section~\ref{sec:measurement-prerequisites} defines \(A(u)\) as this unique increment for participant \(u\).
It defines \(Q(u)\) as the additional value of correct sentence--target pairing over the frozen row-twin control.
Both quantities are aggregated across folds within each participant, making the participant the biological inference unit.

The aligned target does not pass the frozen internal continuation rule.
Let \(\bar{A}\) denote the mean of \(A(u)\) across the \result{e030_participant_n} participants, let \(U^{\mathrm{family}}_{95\%}(\bar{A})\) denote the upper endpoint of its family-adjusted interval, and let \(\tau_{\mathrm{cont}}\) denote the inherited continuation reference.
The result is
\begin{equation}
\begin{aligned}
\bar{A}
&=
\text{\result{e030_target_unique}},
\\
\mathrm{CI}^{\mathrm{family}}_{95\%}(\bar{A})
&=
\text{\result{e030_target_unique_ci}},
\\
U^{\mathrm{family}}_{95\%}(\bar{A})
&=
\text{\result{e030_target_unique_upper}}
<
\tau_{\mathrm{cont}}
=
\text{\result{e030_practical_threshold}}.
\end{aligned}
\label{eq:exact-target-measurability-result}
\end{equation}
Equation~\ref{eq:exact-target-measurability-result} shows that even the family-adjusted upper endpoint remains below the inherited continuation reference.
Only \result{e030_target_positive_n} participant effects are positive, and every participant- and block-leave-out mean remains negative.
The assay therefore supports an internal noncontinuation decision under its frozen conditions.
The reference does not define a universal threshold for biological importance. \evd{E030}

Correct sentence--target pairing remains unresolved.
The frozen row twin preserves the target rows and readout procedure while breaking their pairing with sentences.
The participant-mean aligned-minus-row-twin contrast is \result{e030_target_twin}.
Its family-95\% interval is \result{e030_target_twin_ci}.
\result{e030_target_twin_positive_n} participant contrasts are positive, but the interval spans zero.
The assay therefore does not establish that correct pairing adds predictive value over this frozen row-twin control. \evd{E030}

Figure~\ref{fig:exact-target-measurability} places the two participant-level contrasts on a common scale.
The red guide applies only to the aligned-target increment and marks the inherited internal continuation reference.
Colored squares and horizontal bars show participant means and Bonferroni family-95\% intervals.

\begin{figure}[H]
  \centering
  \resizebox{\linewidth}{!}{\input{figures/exact_target_measurability.tikz}}
  \caption{Exact-target measurability and row-pairing specificity across participants.\evd{E030}}
  \label{fig:exact-target-measurability}
\end{figure}
\FloatBarrier

Together, these results show that exact-target measurability is not demonstrated, while row-pairing specificity remains unresolved, on the fixed sentence set.
They do not exclude total or nuisance-redundant predictivity, information outside the 50 retained target principal components, nonlinear access, or effects under another target transform, cohort, or endpoint.
They also do not causally explain the later student results.
The next subsection therefore asks a separate question: whether training nevertheless produced retained target uptake and student movement, and whether the available comparator attributes those changes to brain-response content.

\subsection{Synthetic-response manipulation checks and attribution status}
\label{sec:results-synthetic-manipulation}

Training changed the saved student even though the preceding exact-target biological prerequisite did not pass.
The first manipulation check asks whether a fresh readout recovers more of the synthetic target from the synthetic-response student than from its seed-matched ordinary-distillation counterpart.
Let \(R^2_{s,a}(T)\) denote held-out WikiText target recovery for technical seed \(s\), training arm \(a\), and target \(T\).
Let \(S\) denote the number of saved technical seeds (\result{e016_seed_n}).
The mean uptake contrast is
\begin{equation}
\widehat{\Delta}_{\mathrm{uptake}}^{\mathrm{Wiki}}
=
\frac{1}{S}
\sum_{s=1}^{S}
\left[
R^2_{s,\mathrm{synthetic}}(T)
-
R^2_{s,\mathrm{KD}}(T)
\right].
\label{eq:results-wiki-uptake}
\end{equation}
Equation~\ref{eq:results-wiki-uptake} measures the increase in linear target recoverability after synthetic-response training relative to ordinary \ac{kd}.
Its estimate is \result{e016_tribe_synth_gain}, with two-sided 95\% seed-axis \(t\) interval \result{e016_tribe_synth_gain_ci}.
The saved procedure therefore passes the WikiText target-uptake check.
These seeds quantify technical stability, not biological or model-population uncertainty. \evd{E016}\evd{E025}

The retained student also moved while measured language quality remained within the declared margin.
Mean movement from the ordinary-distillation student, measured by layer-6 centered relative Frobenius distance, is \result{e025_tribe_movement_mean}.
Every saved technical seed passes the movement check.
Across the direct saved-arm comparisons, the largest absolute language-quality difference is \result{e025_quality_max_gap} bits per byte, below the frozen \result{e025_quality_margin} margin.
These results show that the intervention changed the retained student without a detected language-quality discrepancy larger than the declared margin.
They do not show that brain-response content caused the change or improved language quality. \evd{E025}

Fresh recovery on the Tuckute sentences gives a narrower result.
Absolute target extractability from the synthetic-response students is \result{e030_target_extract}, with Bonferroni family-95\% interval \result{e030_target_extract_ci}.
The incremental synthetic-response-minus-ordinary-distillation estimate is instead \result{e030_retention_delta}, with Bonferroni family-95\% interval \result{e030_retention_delta_ci}.
Thus, the target is linearly recoverable from the trained students on this new substrate, but the analysis does not establish that synthetic-response training increased its recoverability beyond ordinary distillation.
The \result{e030_seed_n} seeds again describe stability of the saved training procedure rather than biological replication. \evd{E030}

The manipulation results cannot be attributed to brain-response content because the available text-derived auxiliary control is not adequately matched.
The comparator audit fails \result{e026_check_split} measured matching criteria fixed before the audit.
Before intervention, ordinary-distillation representations recover mean target \(R^2\) of \result{e026_kd_targets} for the synthetic and text-derived targets, respectively.
This leaves \result{e026_headroom_ratio} times as much remaining mean headroom for the synthetic target.
Required margins also fail for covariance geometry, low-level nuisance predictability, global parameter displacement, and centered-Frobenius retained-student movement.
Initial auxiliary-loss and gradient comparability remain unresolved because those quantities were not retained.
These mismatches can affect apparent learnability and student movement without any difference in brain-response content.
The audit does not establish which mismatch caused the observed cross-target difference. \evd{E026}

Figure~\ref{fig:synthetic-manipulation-attribution} separates these manipulation results from the attribution audit.
Panel~a displays WikiText target uptake, absolute Tuckute target recovery, and incremental Tuckute retention on separate miniature axes because their estimands and scales differ.
Panel~b summarizes the comparator audit and makes the baseline target-recovery mismatch visible.

\begin{figure}[H]
  \centering
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/synthetic_manipulation_results.tikz}}
    \par\smallskip
    \caption{Manipulation and target-retention results on their assay-specific scales.}
    \label{fig:synthetic-manipulation-results}
  \end{subfigure}
  \par\smallskip
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/synthetic_comparator_audit.tikz}}
    \par\smallskip
    \caption{Comparator-audit outcomes and baseline target-recovery mismatch.}
    \label{fig:synthetic-comparator-audit}
  \end{subfigure}
  \caption{Synthetic-response manipulation and attribution evidence.\evd{E016}\evd{E025}\evd{E026}\evd{E030}}
  \label{fig:synthetic-manipulation-attribution}
\end{figure}
\FloatBarrier

The saved block-permuted arm cannot repair this attribution problem.
Its construction leaves some rows aligned and duplicates or omits other rows, so it remains a sensitivity check rather than an exact-null trained comparator. \evd{E016}

These results therefore show that the intervention produced target uptake and retained-student movement.
They do not identify an effect caused specifically by brain-response content.
Synthetic-response training increased held-out WikiText target recovery and changed the retained student at acceptable measured language quality.
The fresh post-training readout on held-out Tuckute sentences supports absolute target recovery but leaves incremental recovery over ordinary distillation unresolved.
Because the text-derived auxiliary-control arm is non-identifying as a comparator, the larger synthetic-target gain remains a within-target learnability result.
The next subsection evaluates biological transfer using independently recorded participant responses while preserving this attribution limit.

\subsection{Saved-student biological-transfer assays}
\label{sec:results-predicted-transfer}

The preceding manipulation checks show that synthetic-response training changed the saved student, but they do not show that the change improves prediction of recorded brain responses.
The direct biological-transfer assay therefore compares each synthetic-response student with its seed-matched ordinary-\ac{kd} student.
Folds and technical seeds are averaged within each participant, and the participant is the biological inference unit (\result{e025_participant_n}).
The participant-level result is
\begin{equation}
\widehat{\Delta}_{\mathrm{direct}}
=
\mbox{\result{e025_tribe_kd}},
\qquad
\mathrm{CI}_{95\%}
=
\mbox{\result{e025_tribe_kd_ci}}.
\label{eq:results-direct-biological-transfer}
\end{equation}
Here, \(\widehat{\Delta}_{\mathrm{direct}}\) is the participant-mean change in held-out unique \(R^2\) produced by synthetic-response training relative to ordinary distillation.
The interval spans zero, so the direct assay does not support participant-general biological transfer in the tested cohort.
It does not establish that the effect is exactly zero, and its scope remains limited to the fixed target, student, layer, nuisance design, sentences, participants, and \ac{roi} endpoint. \evd{E025}

The synthetic-response-versus-text-derived-control contrast is numerically positive, but it does not overturn the direct result:
\begin{equation}
\widehat{\Delta}_{\mathrm{synthetic-text}}
=
\mbox{\result{e025_tribe_kd}}
-
\left(\mbox{\result{e025_textfeat_kd}}\right)
=
\mbox{\result{e025_participant_delta}},
\qquad
\mathrm{CI}_{95\%}
=
\mbox{\result{e025_participant_ci}}.
\label{eq:results-control-transfer-decomposition}
\end{equation}
This decomposition shows that the positive relative contrast arises because the text-derived-control arm is more negative than ordinary distillation, not because the synthetic-response arm has a positive direct effect.
The interval also spans zero.
Moreover, Section~\ref{sec:results-synthetic-manipulation} shows that the text-derived auxiliary-control arm is not adequately matched as a comparator.
The contrast therefore cannot identify an effect of brain-response content. \evd{E025}\evd{E026}

The frozen composed-path analysis asks a different question.
It measures whether recorded-response predictivity can pass through the synthetic-target component recovered from a frozen student.
Figure~\ref{fig:saved-student-biological-transfer}b reports its three participant-level estimands:
\begin{align}
\widehat{O}
&=
\mbox{\result{e030_overlap_abs}},
&
\mathrm{CI}^{\mathrm{family}}_{95\%}(\widehat{O})
&=
\mbox{\result{e030_overlap_abs_ci}},
\\
\widehat{D}
&=
\mbox{\result{e030_overlap_delta}},
&
\mathrm{CI}^{\mathrm{family}}_{95\%}(\widehat{D})
&=
\mbox{\result{e030_overlap_delta_ci}},
\\
\widehat{J}
&=
\mbox{\result{e030_overlap_twin}},
&
\mathrm{CI}^{\mathrm{family}}_{95\%}(\widehat{J})
&=
\mbox{\result{e030_overlap_twin_ci}}.
\end{align}
Here, \(O\) measures absolute aligned predictive overlap, \(D\) measures its increment over ordinary distillation, and \(J\) asks whether that increment is larger for correctly aligned targets than for the frozen row twin.
Every family-adjusted interval spans zero.
The composed route therefore does not demonstrate predictive overlap, incremental overlap over ordinary distillation, or alignment-specific overlap.
These quantities are predictive-path diagnostics, not causal-mediation estimates or replacements for the direct biological-transfer endpoint. \evd{E030}

Figure~\ref{fig:saved-student-biological-transfer} summarizes the direct assay and the composed-path diagnostic after their estimands have been defined.
Panel~a shows the nine direct participant effects and their participant-level interval; the zero line makes the uncertain direction visible without treating folds or technical seeds as additional biological observations.
Panel~b shows the family-adjusted intervals for \(O\), \(D\), and \(J\).

\begin{figure}[H]
  \centering
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/saved_student_biological_transfer.tikz}}
    \par\vspace{0.35em}
    \caption{Direct synthetic-response-versus-ordinary-distillation effects across participants.}
  \end{subfigure}
  \par\smallskip
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/composed_path_diagnostics.tikz}}
    \par\vspace{0.35em}
    \caption{Family-adjusted composed-path diagnostic intervals.}
  \end{subfigure}
  \caption{Direct biological transfer and composed-path diagnostics.\evd{E025}\evd{E030}}
  \label{fig:saved-student-biological-transfer}
\end{figure}
\FloatBarrier

Within the fixed assay, neither the direct saved-student comparison nor the composed-path diagnostics demonstrate participant-general biological transfer.
The direct and composed-path claims are not demonstrated, and the text-derived comparison is non-identifying.
The next subsection asks whether any completed route nevertheless supports a bounded practical-utility claim.

\subsection{Bounded practical-utility evaluation}
\label{sec:results-utility}

The final subsection returns to the recorded-response route, which contains the only completed practical-utility evaluation.
It compares the averaged recorded-response arm with its approximately quality-matched block-permuted-response control across \result{e009_seed_n} technical training seeds.
Equation~\ref{eq:practical-utility-ratio} defines the out-of-domain to in-domain perplexity ratio and the recorded-response-minus-permuted-response contrast, for which negative values favor the recorded-response arm.
Averaging that contrast across seeds gives
\begin{equation}
\overline{\Delta R}_{\mathrm{Pereira}}
=
\mbox{\result{e009_pereira_recorded_permuted}},
\qquad
\overline{\Delta R}_{\mathrm{LeBel}}
=
\mbox{\result{e009_lebel_recorded_permuted}}.
\label{eq:results-practical-utility}
\end{equation}
These are the mean contrasts for Pereira sentences~\autocite{pereira2018} and LeBel story text~\autocite{lebel2023}, respectively.
Neither contrast exceeds its observed-variance 80\%-power minimum detectable effect.
The completed endpoint therefore does not demonstrate a detectable advantage over the permuted-response control. \evd{E009}

Interpreting this downstream result requires a reliable difference in controlled brain predictivity between the two arms.
Equation~\ref{eq:practical-utility-prerequisite} defines this prerequisite alignment contrast as \(A_s\).
Its seed-level summary is
\begin{equation}
\operatorname{mean}_s(A_s)
=
\mbox{\result{e009_fulcrum_mean}},
\qquad
\operatorname{median}_s(A_s)
=
\mbox{\result{e009_fulcrum_median}},
\qquad
\mathrm{MDE}_{80\%}
=
\mbox{\result{e009_fulcrum_mde}}.
\label{eq:results-utility-prerequisite}
\end{equation}
The mean prerequisite alignment contrast is smaller than the observed 80\%-power minimum detectable effect, and the median is smaller still.
Only \result{e009_positive_seed_signs} contrasts are positive.
Seed~0 contributes \result{e009_seed0_gap}, whereas the other seven seeds average approximately \result{e009_other_seven_mean}.
The prerequisite alignment change is therefore not demonstrated under the measured minimum detectable effect.
This conclusion does not establish that the change is absent.
It means that the completed evaluation cannot estimate the utility of a reliably induced brain-response-specific representational change. \evd{E009}

Figure~\ref{fig:bounded-practical-utility} shows both limits.
Panel~a reports the two completed utility contrasts, and Panel~b shows the seed variation and measured sensitivity of the prerequisite alignment contrast.

\begin{figure}[H]
  \centering
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/practical_utility_contrasts.tikz}}
    \par\vspace{0.35em}
    \caption{Recorded-response-minus-permuted-response utility contrasts.}
  \end{subfigure}
  \par\smallskip
  \begin{subfigure}[t]{\textwidth}
    \centering
    \resizebox{\linewidth}{!}{\input{figures/practical_utility_prerequisite.tikz}}
    \par\vspace{0.35em}
    \caption{Seed-level prerequisite alignment contrasts and measured sensitivity.}
  \end{subfigure}
  \caption{Bounded utility endpoint and its prerequisite alignment contrast.\evd{E009}}
  \label{fig:bounded-practical-utility}
\end{figure}
\FloatBarrier

A brain-response-specific practical advantage is not demonstrated for these corpora, this student--teacher pair, and this quality-controlled recorded-response procedure.
The result does not establish a universal practical-utility null, and it does not evaluate the payoff that might follow from an intervention with a reliably established prerequisite alignment change.
Section~\ref{sec:discussion} places this bounded result within the full evidence chain.
Supporting reproductions, stopped routes, and prospective designs remain in Appendix~\ref{app:secondary}.
