\section{Results}
\label{sec:results}

The evidence chain separates a measured signal from a successful intervention.
Table~\ref{tab:results-status} uses \emph{supported} when the declared stage passes, \emph{not demonstrated} when the tested contrast does not support its target claim, \emph{unresolved} when the evidence cannot decide, and \emph{non-identifying comparator} when the available control cannot support attribution to brain-response content.
These labels are scoped to the estimand, assay, and inference unit stated in Sections~\ref{sec:data-targets}--\ref{sec:estimands}.

\begin{table}[H]
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{1}
  \caption{Verdict-first status of the evidence chain. The table reports the strongest evidence and its licensed interpretation, not the experiment chronology.}
  \label{tab:results-status}
  \begin{tabular}{p{26mm} p{26mm} p{50mm} p{43mm}}
    \toprule
Stage & Status & Strongest evidence & Interpretation \\
    \midrule
    Controlled brain predictivity & \textbf{Supported} & Trained representations exceed architecture-matched untrained controls on both recorded-\ac{fmri} substrates; naturalistic gaps are \result{e006_gpt2_gap} and \result{e006_qwen_gap} \evd{E002}\evd{E006}. & Controlled predictivity exists under the tested linear assays; no intervention or population claim follows. \\
    Quality-aware headroom & \textbf{Supported}; source \textbf{unresolved} & Cold-start \ac{kd} leaves a teacher gap of \result{e003_kd_cold_delta} and floor-anchored retention of \result{e003_kd_cold_retention} \evd{E003}. & There is observed room to intervene, but language quality competes with an objective-specific explanation \evd{E015}. \\
    Recorded-target pairing & \textbf{Not demonstrated} & Mean of nine participant medians \result{e008_mean}, with participant $t$ interval \result{e008_ci} \evd{E008}. & The averaged-target trend does not become a participant-general advantage. \\
    Exact-target measurability & \textbf{Not demonstrated}; twin \textbf{unresolved} & Aligned-target increment \result{e030_target_unique}, simultaneous 95\% interval \result{e030_target_unique_ci}; aligned-minus-twin contrast remains uncertain \evd{E030}. & The predicted-target branch misses its first frozen biological prerequisite. \\
    Target uptake and retained-student movement & \textbf{Supported} on WikiText continuity; fresh Tuckute incremental retention \textbf{unresolved} & TRIBE-target gain \result{e016_tribe_synth_gain}, two-sided 95\% seed-axis $t$ interval \result{e016_tribe_synth_gain_ci}; mean retained-student movement \result{e025_tribe_movement_mean} \evd{E016}\evd{E025}\evd{E030}. & Target uptake and retained-student movement occur within the frozen quality margin; they establish neither attribution to neural target content nor transfer. \\
    Brain-response-specific attribution & \textbf{Non-identifying comparator} & The saved text-feature control fails \result{e026_check_split} frozen measured-axis checks \evd{E026}. & Its contrast with TRIBE cannot support attribution to brain-response content. \\
    Biological transfer & \textbf{Not demonstrated} & Direct participant-first TRIBE-minus-\ac{kd} mean \result{e025_tribe_kd}, 95\% interval \result{e025_tribe_kd_ci} \evd{E025}. & No participant-general improvement is supported in the tested cohort. \\
    External utility & \textbf{Not demonstrated} & Tested routes either lack their prerequisite intervention signal or stop at a precondition \evd{E009}\evd{E024}. & No brain-specific neural-target payoff claim is licensed; untested routes remain open. \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Is controlled neural signal present?}
\label{sec:results-signal}

Controlled neural signal is present under both recorded-response assays.
On the Tuckute sentence benchmark, the declared middle layers of every trained model family yield positive nuisance-subtracted held-out predictivity, whereas architecture-matched untrained controls are negative across their seeds and evaluated layers \evd{E002}.
For the primary Qwen2.5-0.5B layer-12 assay, controlled unique $R^2$ is \result{e002_qwen_unique} (mean $\pm$ SD across five contiguous folds), and the architecture-matched trained-minus-untrained contrast is \result{e002_qwen_gap} \evd{E002}.
The five contiguous stimulus folds are the valid partitions for this fixed five-participant-average target; they support a controlled existence result, not participant-level inference.

The signal also survives the naturalistic assay.
For UTS03, GPT-2 and Qwen2.5-0.5B exceed their untrained controls by \result{e006_gpt2_gap} and \result{e006_qwen_gap}, respectively, over \result{e006_reliable_voxels} independently selected voxels; every held-out-story block has the same trained-over-untrained direction \evd{E006}.
Spatially dependent voxels from one participant and story folds with shared training stories are sensitivity dimensions, not independent population replicates; a voxel-resampling interval would pseudo-replicate the biological unit.
The supported claim is therefore within-person predictivity under the fixed story, nuisance, reliability, and readout protocol, not a population effect.

Together, the sentence and story results pass the measurement stage without being pooled into one estimate.
They show that language training adds linearly accessible information about held-out recorded responses across two stimulus formats and target resolutions.
They do not show that the signal can be increased by neural-guided training, that an increase would generalize across people, or that it would improve language behavior.

\subsection{Does compression leave interpretable alignment headroom?}
\label{sec:results-headroom}

Compression leaves observed alignment headroom in the from-scratch distillation assay.
At the fixed Tuckute layers, the cold-start GPT-2 student finishes \result{e003_kd_cold_delta} below GPT-2-medium in controlled brain predictivity and retains \result{e003_kd_cold_retention} of the teacher-to-untrained interval under the recorded bootstrap \evd{E003}.
The random network and pretrained student supply floor and inherited-alignment references, respectively; the cold-start run asks how much of the teacher-to-floor interval \ac{kd} transmits without inherited student features.
This ordering rules out preservation for free in that cold-start run, but it does not isolate why the gap arose.

Language quality is the main competing explanation.
Within the same assay, alignment and log perplexity correlate at Pearson \result{e003_quality_corr} across the heterogeneous trained and reference arms \evd{E003}.
The cold checkpoint also remains substantially worse on held-out language quality than the teacher and warm-start arms, while its absolute alignment estimate changes with the declared \ac{pca} rank.
Cold and warm arms also differ in initialization and optimizer-step count, so their ordering is not a paired causal contrast \evd{E003}.
The teacher gap is therefore observed headroom, not an identified effect of the distillation objective.

The cross-family analysis strengthens the need for quality control without turning quality into a cause.
Across the full range, Pearson $r$ between alignment and tokenizer-comparable log bits per byte is \result{e015_full_corr}, with a six-family cluster-bootstrap 95\% interval of \result{e015_full_ci} \evd{E015}.
Models within a family are not independent, and the six families largely separate into roughly two model generations; the family-cluster interval is therefore not six independent cross-generation confirmations.
Within the capable-model band (\result{e015_capable_n}), Pearson $r$ between alignment and log bits per byte is \result{e015_capable_corr}, with Fisher 95\% interval \result{e015_capable_ci}, which includes zero \evd{E015}.
After conditioning on log parameter count, the partial $r$ between alignment and log bits per byte remains negative at \result{e015_partial_corr}, with Fisher 95\% interval \result{e015_partial_ci}; however, parameter count and language quality are highly collinear, and model families lack common quality support for an identified family residual \evd{E015}.
These tests support quality coupling as a design threat, not a universal law or a causal mechanism.

Controlled predictivity and observed headroom therefore license an intervention test, but only at comparable language quality.
The next branch must compare paired interventions at matched checkpoints or operating points; neither the teacher gap nor a quality correlation can substitute for that contrast.

\subsection{Recorded-target branch: Is there a participant-general advantage?}
\label{sec:results-recorded}

Recorded-target training produces positive point estimates against a block-permuted target when the response is averaged across participants, but the valid fold analysis does not establish a pairing effect.
For Qwen2.5-0.5B at layer 12, the real-minus-permuted point estimate is \result{e004_qwen_specific}; the valid analysis averages seeds within each of five shared stimulus folds before computing the $t$ interval \result{e004_qwen_fold_ci} \evd{E004}.
Treating seed-by-fold cells as independent would pseudo-replicate technical repetitions and shared stimuli; removing the dominant fold reduces the mean to \result{e004_qwen_lofo}.
This supports neither a reliable fold-level effect nor a zero-effect claim, because the interval remains compatible with a small change.

The averaged-target paired run shows the same distinction between a positive trend and an identified advantage.
Its apparent paired gain is \result{e005_apparent}, with a valid five-fold $t$ interval of \result{e005_fold_ci}; one fold contributes most of the mean \evd{E005}.
The valid unit is again the shared stimulus fold after aggregating seed repetitions; treating seeds as independent would pseudo-replicate the training procedure.
Moreover, the optimized response is the cellwise five-participant average: it estimates alignment with the shared stimulus-locked target, not the mean effect in a population of participants.
The block permutation tests whether the correct sentence--response pairing matters under that averaged target; it does not by itself support attribution to neural target content against a geometry-matched nonbrain target.
On the fixed WikiText probe, the real and permuted arms have perplexities \result{e005_real_ppl} and \result{e005_permuted_ppl}, respectively \evd{E005}.
Their proximity is a paired quality check showing no gross imbalance, not a declared quality match.
Comparisons with ordinary \ac{kd} remain descriptive unless checkpoints or curves are matched at a common perplexity.

Participant-first evaluation removes that inferential ambiguity and does not support an advantage.
After taking the median over \result{e008_cells_per_participant} paired seed--fold cells within each valid participant, the cohort mean real-minus-permuted effect is \result{e008_mean}, with participant $t$ interval \result{e008_ci} \evd{E008}.
The shared-fold summary is likewise centered near zero at \result{e008_fold_mean}, with five-fold $t$ interval \result{e008_fold_ci}; the exact participant sign test and leave-one-fold checks do not reject the null \evd{E008}.
The participant analysis had sensitivity below the apparent averaged-target effect, so its result is not explained by merely replacing a large effect with an obviously uninformative assay.
It remains a scoped null for this target, model, objective, cohort, and readout rather than proof that recorded responses can never help training.
Crucially, each participant effect is formed only after aggregating that person's seeds and stimulus folds.
Seed consistency describes the stochastic training procedure, and the shared-fold analysis checks stimulus concentration; neither creates additional biological units.

Averaging diagnostics explain why the two estimands can diverge without invalidating brain encoding as a measurement.
Varying the number and identity of participants in the optimization target does not yield a stable monotone intervention curve once random subsets replace the confounded nested sequence \evd{E010}.
With the encoder frozen and no intervention, however, averaging responses raises controlled encoding predictivity by improving signal-to-noise and emphasizing the shared response component; individual targets already retain positive signal \evd{E014}.
Thus averaging neither creates independent biological replicates nor fabricates an absent encoding signal.
It changes the target and can strengthen measurement, but it cannot turn an averaged-target training trend into participant-general evidence.
The dose diagnostic remains descriptive because its participant subsets overlap and target size changes together with participant identity.
The frozen-encoder diagnostic avoids that intervention ambiguity: any score increase there comes from response construction and the changed estimand, not student optimization.

Changing capacity, loss, substrate, or trainable parameter set does not rescue the tested recorded-target branch (Table~\ref{tab:recorded-variants}).
The table gives only the inferentially relevant disposition.
Objectives and schedules are documented in Appendix~\ref{app:training}, secondary-variant dispositions in Appendix~\ref{app:secondary}, and claim-relevant sensitivities and full numerical tables in Appendix~\ref{app:sensitivities}.

\begin{table}[t]
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3pt}
  \renewcommand{\arraystretch}{1}
  \caption{Recorded-target intervention variants. A nonpass is scoped to the named manipulation and valid inference unit.}
  \label{tab:recorded-variants}
  \begin{tabular}{p{29mm} p{32mm} p{46mm} p{40mm}}
    \toprule
    Variant & Target and unit & Strongest valid read & Disposition \\
    \midrule
    Higher-capacity \ac{lora} & Tuckute participant targets; participant unit & Mean \result{e011_mean}, participant $t$ interval \result{e011_ci} \evd{E011}. & More adapter capacity at the fixed loss weight does not rescue the participant effect; this is not a test of a stronger manipulation. \\
    Contrastive objective & Tuckute participant targets; participant unit & Mean \result{e013b_mean}, participant $t$ interval \result{e013b_ci} \evd{E013}. & Changing the loss does not rescue the effect at five-\ac{roi} granularity. \\
    Naturalistic voxelwise \ac{lora} & LeBel story responses; within-person probes & The required real-over-permuted manipulation check fails in the tested cells \evd{E013}. & Non-identifying lever failure, not a naturalistic population null. \\
    Full fine-tuning & LeBel story responses; three participants & Gentle quality-preserving training shows no reliable real-over-permuted gain; aggressive training also incurs forgetting \evd{E017}. & The tested full-parameter route does not rescue the effect; no nine-participant inference is implied. \\
    \bottomrule
  \end{tabular}
\end{table}

The narrow conclusion is that none of the tested recorded-response objectives demonstrates a participant-general advantage attributable to correct stimulus--response pairing.
The variants rule out specific explanations---insufficient adapter rank at a fixed weight, one five-\ac{roi} loss form, and the tested naturalistic or full-parameter routes---without closing all recorded-target designs.
The averaging diagnostic also prevents a broader overcorrection: stronger prediction of an averaged response remains a valid measurement of the shared component, even though it cannot support a claim about mean participant benefit.
With controlled signal and headroom established but the recorded-target intervention unsupported, the predicted-target branch must begin again at exact-target measurability before target uptake can matter.

\subsection{Predicted-target branch I: Measurability, uptake, and attribution}
\label{sec:results-predicted-identification}

The predicted-target branch misses its first biological prerequisite: the exact target fails the frozen practical linear-measurability rule.
With train-fold \ac{pca}-50 and participant as the inference unit (\result{e030_participant_n}), the estimator is the participant mean of the aligned TRIBE target's held-out unique $R^2$ increment beyond the imageability-complete nuisance model.
That increment is \result{e030_target_unique}, with Bonferroni family-95\% two-sided $t$ interval \result{e030_target_unique_ci}; the simultaneous upper endpoint is below the frozen \result{e030_practical_threshold} continuation threshold \evd{E030}.
This result excludes a practically relevant unique linear increment only under the fixed target transform, nuisance model, sentence set, cohort, and readout.
It does not exclude total or nuisance-redundant target predictivity, nonlinear access, or another target transform or cohort.
The aligned-minus-frozen-row-twin contrast is \result{e030_target_twin}, with family-95\% interval \result{e030_target_twin_ci}, so row-specific target advantage remains unresolved \evd{E030}.

Target uptake and retained-student movement are supported in the saved WikiText continuity assay, not as fresh Tuckute incremental retention.
Using the saved technical training seeds (\result{e016_seed_n}), the fresh-readout TRIBE-target gain over seed-matched \ac{kd} is \result{e016_tribe_synth_gain} target $R^2$, with two-sided 95\% seed-axis $t$ interval \result{e016_tribe_synth_gain_ci} \evd{E016}\evd{E025}.
Mean centered relative-Frobenius retained-student movement from \ac{kd} is \result{e025_tribe_movement_mean}, and every saved technical seed passes the movement check.
The maximum absolute language-quality difference is \result{e025_quality_max_gap} bits per byte, below the frozen \result{e025_quality_margin} margin, and every direct comparison passes \evd{E025}.
Training parameters are documented in Appendix~\ref{app:training}, while claim-relevant representation sensitivities appear in Appendix~\ref{app:sensitivities}.
In the fresh Tuckute \ac{pca}-50 assay, absolute target extractability from TRIBE-trained representations is \result{e030_target_extract}, with Bonferroni family-95\% interval \result{e030_target_extract_ci}, whereas the incremental TRIBE-minus-\ac{kd} retention estimate is \result{e030_retention_delta}, with Bonferroni family-95\% interval \result{e030_retention_delta_ci} \evd{E030}.
Thus absolute recovery is stable across the technical seed unit (\result{e030_seed_n}), but fresh Tuckute incremental retention remains unresolved.
Training seeds assess the stability of the saved procedure; they are not biological or model-population replicates.
The saved block-permutation arm is only a sensitivity analysis because its permutation duplicates or omits rows, so it cannot supply an exact-null attribution claim \evd{E016}.

The passed manipulation checks do not establish attribution to neural target content because the saved nonbrain comparator is non-identifying.
The projected text-feature arm fails \result{e026_check_split} frozen measured-axis checks \evd{E026}.
Before intervention, mean \ac{kd}-only target $R^2$ is \result{e026_kd_targets} for TRIBE and text features, respectively, leaving \result{e026_headroom_ratio} times as much mean headroom for TRIBE.
Frozen margins also fail for covariance geometry, low-level nuisance predictability, global parameter displacement, and centered-Frobenius retained-student movement.
Initial auxiliary-loss and gradient comparability remains unresolved because those values were not retained.
Same-teacher redundancy is consistent with the text-feature arm's high baseline fit, but the audit does not identify that or any other mismatch as the cause.
The larger TRIBE-target gain therefore remains a valid within-target learnability result and a numerical cross-target difference, not evidence for attribution of the change to neural target content \evd{E016}\evd{E026}.

\subsection{Predicted-target branch II: Biological transfer}
\label{sec:results-predicted-transfer}

After failed or unresolved identification prerequisites, the later biological endpoints describe the saved interventions but cannot establish biological transfer attributable to neural target content.
The cleaner direct endpoint compares TRIBE training with ordinary \ac{kd} separately for each complete participant (\result{e025_participant_n}).
Its participant-mean unique-$R^2$ difference is \result{e025_tribe_kd}, with two-sided 95\% participant $t$ interval \result{e025_tribe_kd_ci} \evd{E025}.
This near-zero estimate does not support participant-general improvement in the tested cohort, but it neither proves an exactly zero effect nor generalizes beyond the fixed target, model, layer, nuisance design, and sentence/\ac{roi} endpoint.

The TRIBE-minus-text-feature contrast is numerically positive but does not support attribution to neural target content.
Its participant mean is \result{e025_participant_delta}, with two-sided 95\% participant $t$ interval \result{e025_participant_ci}; the text-feature-minus-\ac{kd} mean is itself \result{e025_textfeat_kd} \evd{E025}.
The positive relative mean therefore does not represent a positive direct TRIBE effect, and the comparator mismatches in Section~\ref{sec:results-predicted-identification} prevent attribution to neural target content \evd{E026}.

The frozen participant-level composed student-to-target-to-response route also does not establish transfer: every family-95\% interval spans zero.
Within this route, at the participant inference unit (\result{e030_participant_n}), the TRIBE-minus-\ac{kd} overlap increment is \result{e030_overlap_delta}, with Bonferroni family-95\% interval \result{e030_overlap_delta_ci}; absolute overlap and the aligned-minus-frozen-twin overlap increment are deferred to Appendix~\ref{app:sensitivities} \evd{E030}.
Because exact-target measurability fails first and twin specificity remains unresolved, these overlap quantities remain downstream descriptive diagnostics rather than independent evidence of transfer.

The first defensible predictive failure is therefore localized at exact-target practical linear measurability, before student-to-brain overlap.
That ordering does not make the measurability result a causal explanation of the near-zero participant transfer: the assay does not test every accessible form of target information, and it does not identify which mechanism produced the later endpoint \evd{E025}\evd{E030}.

Figure~\ref{fig:participant-evidence} aligns the participant-level contrasts at the recorded-target pairing, exact-target measurability, and direct-transfer stages without pooling their estimands or scales.

\begin{figure}[t]
  \centering
  \input{figures/participant_evidence.tikz}
\caption{Participant evidence at three stages. Grey points are the same nine participants; in panel~a each point is the participant median over \result{e008_cells_per_participant} paired seed--fold cells, and the black square is the cohort mean over those nine medians. Black squares and bars otherwise show participant means and the stated 95\% intervals. Panel~a compares recorded-target with matched permuted-target training (\result{e008_mean}, \result{e008_ci}). Panel~b uses its own scale: its top row is the exact predicted target's increment above nuisance (\result{e030_target_unique}, \result{e030_target_unique_ci}), and its bottom row is aligned target minus frozen row twin (\result{e030_target_twin}, \result{e030_target_twin_ci}). Panel~c compares predicted-target training with ordinary \ac{kd} (\result{e025_tribe_kd}, \result{e025_tribe_kd_ci}) on the same scale as panel~a. The red line applies only to panel~b's top row; it is the inherited internal continuation reference \result{e030_practical_threshold}, not an external importance threshold. All four intervals span zero; additionally, panel~b's top-row simultaneous upper endpoint falls below its internal reference under the fixed assay. The intervals treat participants as the population unit after any nested seed and fold aggregation. These results localize nonpasses under the named assays; they neither prove universal nulls nor attribute a contrast to brain-response content. \evd{E008}\evd{E025}\evd{E030}}
  \label{fig:participant-evidence}
\end{figure}
\FloatBarrier

\subsection{External utility and secondary routes}
\label{sec:results-utility}

No completed route establishes a brain-specific neural-target utility claim, but the routes fail at different prerequisites.
The quality-controlled averaged-target test estimates the paired out-of-domain to in-domain perplexity ratio on Pereira text~\autocite{pereira2018} and LeBel text~\autocite{lebel2023}, not raw out-of-domain perplexity.
Its mediator is the paired real-minus-block-permuted change in in-domain controlled brain alignment.
Across the technical training seeds (\result{e009_seed_n}), that mediator has mean \result{e009_fulcrum_mean}, median \result{e009_fulcrum_median}, and an 80\%-power minimum detectable effect of \result{e009_fulcrum_mde}; it is therefore unresolved, and no downstream ratio contrast exceeds its measured minimum detectable effect \evd{E009}.
\result{e009_positive_seed_signs} seed contrasts are positive; seed~0 contributes \result{e009_seed0_gap}, whereas the other seven average approximately \result{e009_other_seven_mean}.
The utility result is consequently bounded to these corpora and this student--teacher pair rather than informative about a successful intervention.

Reduced reproductions, cognitive-target probes, and privileged-information routes provide scoped diagnostics rather than a common utility test.
The reduced Negi-style adaptation \autocite{negi2025} and single-participant Moussa-style setup \autocite{moussa2025,moussa2025b} do not yield their target benefits; the former is a scoped local lever diagnostic, while the latter is a weak reduced non-reproduction, neither corroboration nor refutation of the original multi-participant result \evd{E019}\evd{E022}.
A content-free shape-matched control preserves the supported generic target-shape regularization finding while removing cognition-specific attribution \evd{E021}.
The shared-response ceiling and natural-reading privileged-target routes stop at failed instruments or preconditions \evd{E020}\evd{E024}.
Route-specific designs and failure details are deferred to Appendix~\ref{app:secondary}.

Unexecuted prospective designs contribute no evidence to this conclusion.
The completed evidence therefore supports no brain-specific neural-target utility claim under the tested models, targets, controls, and endpoints, but it does not establish a universal utility null.
Controlled neural signal, target uptake, and retained-student movement are observable; the tested chain loses identifying support before participant-general biological transfer and external benefit.
Section~\ref{sec:discussion} examines what that ordered failure localizes and how the control design should change.
