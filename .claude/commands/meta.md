---
description: Enter the meta stance — build or maintain the apparatus (tooling, methodology, records). Touches no number, flips no rung.
argument-hint: <what to build/fix, e.g. "add a hook" or "reconcile the ladder">
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Task, Skill
---

Enter the **meta** stance. Load the `stances` skill and follow `.claude/skills/stances/modes/meta.md`
on: $ARGUMENTS

- The hard line: no science number produced or consumed, no rung moved. If it starts needing a number,
  re-enter `/work` or `/interpret` and say so.
- Add structure only when its absence costs truth; a workflow reconstructed twice earns one. Record real
  decisions in `decisions/decisions.md`; commit atomically.
