---
description: Enter the meta stance — build or maintain the apparatus without producing or adjudicating science.
argument-hint: <what to build, remove, or repair>
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Task, Skill
---

Enter the **meta** stance. Load the `stances` skill and follow `.claude/skills/stances/modes/meta.md`
on: $ARGUMENTS

- The hard line: no science number produced or adjudicated. If it starts needing a number,
  re-enter `/work` or `/interpret` and say so.
- Add structure only when its absence costs truth; prefer deleting obsolete machinery. Record real
  decisions in `decisions/decisions.md`; commit atomically.
